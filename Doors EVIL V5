-- "Doors_Horror_V5_Remade"AutoUpdate.GainAccess("https.get.controluser.zinxc_official.manage.script('doorshorrorautoupdate'))
-- Script Made By: ZinXC Official (Script Manager, Script Editing, Modify) | Akito Samaka (Models, Scriping Builder, Composer)

-- DOORS HORROR MODE V5 REMADE (Uncompleted Version)
-- Load First
-- Getting Started
if game.ReplicatedStorage.GameData.LatestRoom.Value > 0 then 
firesignal(game.ReplicatedStorage.EntityInfo.Caption.OnClientEvent, "<Error x40023: Run Script Failed>")
	game.ReplicatedStorage.GameStats["Player_".. game.Players.LocalPlayer.Name].Total.DeathCause.Value = "Lỗi"
	firesignal(game.ReplicatedStorage.EntityInfo.DeathHint.OnClientEvent, {"Bạn Phải Cần Chạy Script Từ Phòng Số 0","Hãy Nhớ Điều Này"},'Blue')
	game.Players.LocalPlayer.Character:FindFirstChildWhichIsA("Humanoid"):TakeDamage(100)  
	return
end

game.Lighting.MainColorCorrection.TintColor = Color3.fromRGB(255, 255, 255)
game.Lighting.MainColorCorrection.Contrast = 0.2
game.Lighting.MainColorCorrection.Saturation = 1.5
firesignal(game.ReplicatedStorage.EntityInfo.Caption.OnClientEvent, "Tải Thành Công")
-- Replipication Message Achivement
--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "DOORS EVIL V5 RELEASE",
    Desc = "Đã Load Thành Công Doors EVIL V5 Release",
    Reason = "Just Explore Something...",
    Image = "https://static.wikia.nocookie.net/backrooms/images/3/31/Smiler_Close-Up.png/revision/latest?cb=20210312202418",
})
-- Sprint Mod
local Parent = game.Players.LocalPlayer.PlayerGui
local Sprint = Instance.new("Frame")
local ImageLabel = Instance.new("ImageLabel")
local UICorner = Instance.new("UICorner")
local UIPadding = Instance.new("UIPadding")
local Bar = Instance.new("Frame")
local UICorner_2 = Instance.new("UICorner")
local UIPadding_2 = Instance.new("UIPadding")
local Fill = Instance.new("Frame")
local UICorner_3 = Instance.new("UICorner")

--Properties:

local StaminaGui = Instance.new("ScreenGui")

--Properties:

StaminaGui.Name = "StaminaGui"
StaminaGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")
StaminaGui.Enabled = false
StaminaGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

Sprint.Name = "Sprint"
Sprint.Parent = StaminaGui
Sprint.AnchorPoint = Vector2.new(0, 1)
Sprint.BackgroundColor3 = Color3.fromRGB(0, 0, 255)
Sprint.BackgroundTransparency = 1.000
Sprint.Position = UDim2.new(0.931555569, 0, 0.987179458, 0)
Sprint.Size = UDim2.new(0.0556001104, 0, 0.0756410286, 0)
Sprint.SizeConstraint = Enum.SizeConstraint.RelativeYY
Sprint.ZIndex = 1005

ImageLabel.Parent = Sprint
ImageLabel.BackgroundColor3 = Color3.fromRGB(255, 222, 189)
ImageLabel.Size = UDim2.new(1, 0, 1, 0)
ImageLabel.SizeConstraint = Enum.SizeConstraint.RelativeYY
ImageLabel.Visible = false

UICorner.CornerRadius = UDim.new(1, 0)
UICorner.Parent = ImageLabel

UIPadding.Parent = Sprint
UIPadding.PaddingBottom = UDim.new(0.300000012, -5)
UIPadding.PaddingLeft = UDim.new(0.0199999996, 0)
UIPadding.PaddingRight = UDim.new(0.0500000007, -15)
UIPadding.PaddingTop = UDim.new(0.300000012, -5)

Bar.Name = "Bar"
Bar.Parent = Sprint
Bar.AnchorPoint = Vector2.new(0, 0.5)
Bar.BackgroundColor3 = Color3.fromRGB(56, 46, 39)
Bar.BackgroundTransparency = 0.700
Bar.Position = UDim2.new(-2.72600269, 0, 0.499999672, 0)
Bar.Size = UDim2.new(3.60599804, 0, 0.600000083, 0)
Bar.ZIndex = 0

UICorner_2.CornerRadius = UDim.new(0.25, 0)
UICorner_2.Parent = Bar

UIPadding_2.Parent = Bar
UIPadding_2.PaddingBottom = UDim.new(0, 4)
UIPadding_2.PaddingLeft = UDim.new(0, 4)
UIPadding_2.PaddingRight = UDim.new(0, 4)
UIPadding_2.PaddingTop = UDim.new(0, 4)

Fill.Name = "Fill"
Fill.Parent = Bar
Fill.AnchorPoint = Vector2.new(0, 0.5)
Fill.BackgroundColor3 = Color3.fromRGB(213, 185, 158)
Fill.Position = UDim2.new(0, 0, 255.5, 0)
Fill.Size = UDim2.new(1, 0, 1, 0)
Fill.ZIndex = 2

UICorner_3.CornerRadius = UDim.new(0.25, 0)
UICorner_3.Parent = Fill

local erm = Instance.new("ScreenGui")
local ImageLabel = Instance.new("ImageLabel")
erm.IgnoreGuiInset = true
erm.Name = "erm"
erm.Parent = Parent
erm.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

ImageLabel.Parent = erm
ImageLabel.BackgroundColor3 = Color3.fromRGB(0, 0, 255)
ImageLabel.BackgroundTransparency = 1.000
ImageLabel.Size = UDim2.new(1, 0, 0.998717964, 0)
ImageLabel.Image = "rbxassetid://190596490"
ImageLabel.ImageColor3 = Color3.fromRGB(0, 0, 255)

ImageLabel.ImageTransparency = 1

-- Services

local Players = game:GetService("Players")
local UIS = game:GetService("UserInputService")

-- Variables

local Plr = Players.LocalPlayer
local Char = Plr.Character or Plr.CharacterAdded:Wait()
local Hum = Char:WaitForChild("Humanoid")

local stamina, staminaMax = 75, 75
local sprintTime = 4
local cooldown = false

local ModuleScripts = {
	MainGame = require(Plr.PlayerGui.MainUI.Initiator.Main_Game),
}

-- Setup

local nIdx; nIdx = hookmetamethod(game, "__newindex", newcclosure(function(t, k, v)
	if k == "WalkSpeed" then
		if ModuleScripts.MainGame.chase then
			v = ModuleScripts.MainGame.crouching and 15 or 22
		elseif ModuleScripts.MainGame.crouching then
			v = 8
		else
			v = isSprinting and 20 or 12
		end
	end

	return nIdx(t, k, v)
end))

-- Scripts

sprintTime = math.max(sprintTime - 1, 1)
local zerostamtween = game.TweenService:Create(ImageLabel,TweenInfo.new(12),{ImageTransparency = 0})
UIS.InputBegan:Connect(function(key, gameProcessed)
	if not gameProcessed and key.KeyCode == Enum.KeyCode.F and not cooldown and not ModuleScripts.MainGame.crouching then
		-- Sprinting

		isSprinting = true
		Hum:SetAttribute("SpeedBoost",4)
		zerostamtween:Play()
		while UIS:IsKeyDown(Enum.KeyCode.F) and stamina > 0 do
			stamina = math.max(stamina - 1, 0)
			Fill.Size = UDim2.new(1 / staminaMax * stamina, 1, 1, 0)
			task.wait(sprintTime / 75)
			
		end

		-- Reset
		zerostamtween:Pause()
		isSprinting = false
		Hum:SetAttribute("SpeedBoost",0)
		game.TweenService:Create(ImageLabel,TweenInfo.new(1),{ImageTransparency = 1}):Play()
		Hum.WalkSpeed = 12

		if stamina == 0 then
			-- Cooldown
firesignal(game.ReplicatedStorage.EntityInfo.Caption.OnClientEvent, "< Bạn Bị Đuối Sức >")
			game.TweenService:Create(ImageLabel,TweenInfo.new(0.3),{ImageTransparency = 0}):Play()
			wait(0.3)
			game.TweenService:Create(ImageLabel,TweenInfo.new(10),{ImageTransparency = 1}):Play()
			cooldown = true
function createSound(id, volume, parent)
	assert(id, "sound id wasn't given")
	id = tostring(id):gsub("%D", "") --only keeping the numbers
	assert(id ~= "", "sound id isn't a number") 
	volume = volume or 2 --0.5 is the default volume
	parent = parent or workspace --workspace is the default parent
	local Sound = Instance.new("Sound") 
	Sound.SoundId = "rbxassetid://"..id 
	Sound.Volume = volume 
	Sound.Parent = parent 
	return Sound 
end
local Sound = createSound("rbxassetid://1021550487")
Sound.Looped = false 
Sound:Play() 
wait(1)
function createSound(id, volume, parent)
	assert(id, "sound id wasn't given")
	id = tostring(id):gsub("%D", "") --only keeping the numbers
	assert(id ~= "", "sound id isn't a number") 
	volume = volume or 2 --0.5 is the default volume
	parent = parent or workspace --workspace is the default parent
	local Sound = Instance.new("Sound") 
	Sound.SoundId = "rbxassetid://"..id 
	Sound.Volume = volume 
	Sound.Parent = parent 
	return Sound 
end
local Sound = createSound("rbxassetid://1021550487")
Sound.Looped = false 
Sound:Play()
wait(1)
function createSound(id, volume, parent)
	assert(id, "sound id wasn't given")
	id = tostring(id):gsub("%D", "") --only keeping the numbers
	assert(id ~= "", "sound id isn't a number") 
	volume = volume or 2 --0.5 is the default volume
	parent = parent or workspace --workspace is the default parent
	local Sound = Instance.new("Sound") 
	Sound.SoundId = "rbxassetid://"..id 
	Sound.Volume = volume 
	Sound.Parent = parent 
	return Sound 
end
local Sound = createSound("rbxassetid://1021550487")
Sound.Looped = false 
Sound:Play()  
wait(1.5)
function createSound(id, volume, parent)
	assert(id, "sound id wasn't given")
	id = tostring(id):gsub("%D", "") --only keeping the numbers
	assert(id ~= "", "sound id isn't a number") 
	volume = volume or 2 --0.5 is the default volume
	parent = parent or workspace --workspace is the default parent
	local Sound = Instance.new("Sound") 
	Sound.SoundId = "rbxassetid://"..id 
	Sound.Volume = volume 
	Sound.Parent = parent 
	return Sound 
end
local Sound = createSound("rbxassetid://1021550487")
Sound.Looped = false 
Sound:Play()  
wait(1)
function createSound(id, volume, parent)
	assert(id, "sound id wasn't given")
	id = tostring(id):gsub("%D", "") --only keeping the numbers
	assert(id ~= "", "sound id isn't a number") 
	volume = volume or 2 --0.5 is the default volume
	parent = parent or workspace --workspace is the default parent
	local Sound = Instance.new("Sound") 
	Sound.SoundId = "rbxassetid://"..id 
	Sound.Volume = volume 
	Sound.Parent = parent 
	return Sound 
end
local Sound = createSound("rbxassetid://1021550487")
Sound.Looped = false 
Sound:Play()  
wait(1)
function createSound(id, volume, parent)
	assert(id, "sound id wasn't given")
	id = tostring(id):gsub("%D", "") --only keeping the numbers
	assert(id ~= "", "sound id isn't a number") 
	volume = volume or 2 --0.5 is the default volume
	parent = parent or workspace --workspace is the default parent
	local Sound = Instance.new("Sound") 
	Sound.SoundId = "rbxassetid://"..id 
	Sound.Volume = volume 
	Sound.Parent = parent 
	return Sound 
end
local Sound = createSound("rbxassetid://1021550487")
Sound.Looped = false 
Sound:Play()  
			for i = 1, staminaMax, 1 do
				stamina = i
				Fill.Size = UDim2.new(1 / staminaMax * i, 1, 1, 0)

				task.wait(sprintTime / 50)
			end

			cooldown = false
		else
			-- Refill
			cooldown = false
			Spawn(function()
			    --wait(1)
			    cooldown = false
			end)
			game.TweenService:Create(ImageLabel,TweenInfo.new(1),{ImageTransparency = 1}):Play()
			while not UIS:IsKeyDown(Enum.KeyCode.F) do
				stamina = math.min(stamina + 1, staminaMax)
				Fill.Size = UDim2.new(1 / staminaMax * stamina, 1, 1, 0)

				task.wait(sprintTime / 50)
			end
		end        
	end
end)
Hum:SetAttribute("SpeedBoost",0)
Hum.WalkSpeed = 12
--Setup Fog
game.Lighting.FogEnd = "100"
game.Lighting.FogStart = "50"
game.Lighting.FogColor = Color3.fromRGB(144, 184, 240)
-- Setup Audio And Sound
function createSound(id, volume, parent)
	assert(id, "sound id wasn't given")
	id = tostring(id):gsub("%D", "") --only keeping the numbers
	assert(id ~= "", "sound id isn't a number") 
	volume = volume or 1.5 --0.5 is the default volume
	parent = parent or workspace --workspace is the default parent
	local Sound = Instance.new("Sound") 
	Sound.SoundId = "rbxassetid://"..id 
	Sound.Volume = volume 
	Sound.Parent = parent 
	return Sound 
end
local Sound = createSound("rbxassetid://9113731969")
Sound.Looped = true 
Sound:Play() 
game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
-- Move Disabled For Wait Completed Notification Modding
local LocalPlayer = game:GetService("Players").LocalPlayer
local Controls = require(LocalPlayer.PlayerScripts.PlayerModule):GetControls()

Controls:Disable()
-- WAIT
local CameraShaker = require(game.ReplicatedStorage.CameraShaker)
local camara = game.Workspace.CurrentCamera
local camShake = CameraShaker.new(Enum.RenderPriority.Camera.Value, function(shakeCf)
	camara.CFrame = camara.CFrame * shakeCf
end)
camShake:Start()
camShake:ShakeOnce(10,3,0.1,6,2,0.5)
-- Notification Modding
firesignal(game.ReplicatedStorage.EntityInfo.Caption.OnClientEvent, "Script Tự Động Đồng Bộ Theo Số Phòng")
wait(3.5)
firesignal(game.ReplicatedStorage.EntityInfo.Caption.OnClientEvent, "Được Làm Bởi ZinXC Official (Script Builder, Composer , Enities Creator)")
wait(3.5)
firesignal(game.ReplicatedStorage.EntityInfo.Caption.OnClientEvent, "Đây Là Bản Doors EVIL Cuối Cùng Sẽ Không Bao Giờ Update Và Fix Thêm Nữa")
wait(3.5)
firesignal(game.ReplicatedStorage.EntityInfo.Caption.OnClientEvent, "Cảm Ơn Những Người Bạn Hạ Long Của Tôi: LegendCat, SeekLoiz, QeenLoiz, SadBoizz Vì Đã Đô Nết Cho Mình")
-- Finish Notification Modding Script
local LocalPlayer = game:GetService("Players").LocalPlayer
local Controls = require(LocalPlayer.PlayerScripts.PlayerModule):GetControls()

Controls:Enable()
---------------------------------
-- Guide Sprint Notification
wait(10)
firesignal(game.ReplicatedStorage.EntityInfo.Caption.OnClientEvent, "Giữ F để chạy")
wait(3)
firesignal(game.ReplicatedStorage.EntityInfo.Caption.OnClientEvent, "Nó có thể tiêu hao sức của bạn")
-- Finished Guide Sprint Notification
-- Entity Scripting Info
-- Savage Smile
---------------------------------
-- Checkpoint Doors 2
wait(2)
game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
firesignal(game.ReplicatedStorage.EntityInfo.Caption.OnClientEvent, "Chế Độ Này Hỗ Trợ Multiplayer Bạn Có Thể Mời Bạn Bè Của Bạn Vào Chs Nhé")
-- Checkpoint Doors 3
game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
-- Checkpoint Doors 4
game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
-- Checkpoint Doors 5
game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
-- Checkpoint Doors 6
game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
-- Checkpoint Doors 7
game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
-- Checkpoint Doors 8
game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
-- Checkpoint Doors 9
game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
-- Checkpoint Doors 10
local Creator = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors%20Entity%20Spawner/Source.lua"))()

-- Create entity

local entity = Creator.createEntity({
    CustomName = "A-50", -- Custom name of your entity
    Model = "rbxassetid://11470738519", -- Can be GitHub file or rbxassetid
    Speed = 100, -- Percentage, 100 = default Rush speed
    DelayTime = 3, -- Time before starting cycles (seconds)
    HeightOffset = 0,
    CanKill = true,
    BreakLights = false,
    KillRange = 40,
    BackwardsMovement = false,
    FlickerLights = {
        false, -- Enabled
        2.5, -- Time (seconds)
    },
    Cycles = {
        Min = 1,
        Max = 1
    },
    CamShake = {
        true, -- Enabled
        {5, 15, 0.1, 1}, -- Shake values (don't change if you don't know)
        100, -- Shake start distance (from Entity to you)
    },
    Jumpscare = {
        false, -- Enabled ('false' if you don't want jumpscare)
        {
            Image1 = "https://tr.rbxcdn.com/1e7945796277100aec5f98858407b523/420/420/Decal/Png", -- Image1 url
            Image2 = "https://tr.rbxcdn.com/1e7945796277100aec5f98858407b523/420/420/Decal/Png", -- Image2 url
            Shake = false,
            Sound1 = {
                5351101493, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Sound2 = {
                5351101493, -- SoundId
                { Volume = 5 }, -- Sound properties
            },
            Flashing = {
                true, -- Enabled
                Color3.fromRGB(6, 38, 135), -- Color
            },
            Tease = {
                false, -- Enabled ('false' if you don't want tease)
                Min = 1,
                Max = 3,
            },
        },
    },
    CustomDialog = {"..."}, -- Custom death message (can be as long as you want)
})

-----[[ Advanced ]]-----

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity:", entity, "has entered room:", room)
    Speed = 0
    wait(2)
    Speed = 170
end

entity.Debug.OnEntitySpawned = function(entityModel)
    print("Entity has spawned:", entityModel)
local bruh = workspace.Rebound.Torso
local TweenService = game:GetService("TweenService")
local spawn = Instance.new("Sound")
spawn.Parent = bruh
spawn.Name = "ReboundSpawn"
spawn.SoundId = "rbxassetid://9114221327"
spawn.Volume = 5
spawn.RollOffMaxDistance = 10000
spawn.RollOffMinDistance = 450
spawn:Play()
    local move = GetGitSound("https://github.com/check78/worldcuuuup/blob/main/DoomBegin.mp3?raw=true","Reboun")
    move.Parent = bruh
    move.Name = "ReboundMoving"
    move.Volume = 0
    move.Looped = true
local vroom = TweenService:Create(move, TweenInfo.new(2),{Volume = 0.2})
    local distort = Instance.new("DistortionSoundEffect")
    distort.Level = 0.75
    distort.Parent = move
	move.RollOffMaxDistance = 200
	move.RollOffMinDistance = 100
    local tree = Instance.new("TremoloSoundEffect")
    tree.Depth = 1
    tree.Duty = 1
    tree.Frequency = 5
    tree.Parent = move
    local eq = Instance.new("EqualizerSoundEffect")
    eq.HighGain = -60
    eq.MidGain = 10
    eq.LowGain = 10
    eq.Parent = move
	wait(1)
    vroom:Play()
	move:Play()
end

entity.Debug.OnEntityDespawned = function(entityModel)
    print("Entity has despawned:", entityModel)
end

entity.Debug.OnEntityStartMoving = function(entityModel)
    print("Entity has started moving:", entityModel)
end

entity.Debug.OnEntityFinishedRebound = function(entityModel)
    print("Entity finished rebound:", entityModel)
	
end

entity.Debug.OnDeath = function()
    warn("You died.")
local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(15),{TintColor = Color3.fromRGB(255, 0, 0)})
TW:Play()
    firesignal(game.ReplicatedStorage.EntityInfo.DeathHint.OnClientEvent, {"..."}, "Blue")

	local ReSt = game.ReplicatedStorage
	local Plr = game.Players.LocalPlayer

		scare:Play()
	    ReSt.GameStats["Player_".. Plr.Name].Total.DeathCause.Value = "Rebound"
		wait(0.5)
		scare:Destroy()
end

------------------------

-- Run the created entity
Creator.runEntity(entity)
function createSound(id, volume, parent)
	assert(id, "sound id wasn't given")
	id = tostring(id):gsub("%D", "") --only keeping the numbers
	assert(id ~= "", "sound id isn't a number") 
	volume = volume or 0.5 --0.5 is the default volume
	parent = parent or workspace --workspace is the default parent
	local Sound = Instance.new("Sound") 
	Sound.SoundId = "rbxassetid://"..id 
	Sound.Volume = volume 
	Sound.Parent = parent 
	return Sound 
end
local Sound = createSound("rbxassetid://9120829080")
Sound.Looped = false 
Sound:Play() 
local CameraShaker = require(game.ReplicatedStorage.CameraShaker)
local camara = game.Workspace.CurrentCamera
local camShake = CameraShaker.new(Enum.RenderPriority.Camera.Value, function(shakeCf)
	camara.CFrame = camara.CFrame * shakeCf
end)
camShake:Start()
camShake:ShakeOnce(15,8.8,0,2,1,6)
game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
-- Start Entity
wait(90)
game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
game.Lighting.MainColorCorrection.TintColor = Color3.fromRGB(255, 0, 0)
game.Lighting.MainColorCorrection.Contrast = 0.2
game.Lighting.MainColorCorrection.Saturation = -0.7
local tween = game:GetService("TweenService")
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Contrast = 0}):Play()
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Saturation = 0}):Play()
local TweenService = game:GetService("TweenService")
local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5),{TintColor = Color3.fromRGB(0, 0, 255)})
TW:Play()

function createSound(id, volume, parent)
	assert(id, "sound id wasn't given")
	id = tostring(id):gsub("%D", "") --only keeping the numbers
	assert(id ~= "", "sound id isn't a number") 
	volume = volume or 2 --0.5 is the default volume
	parent = parent or workspace --workspace is the default parent
	local Sound = Instance.new("Sound") 
	Sound.SoundId = "rbxassetid://"..id 
	Sound.Volume = volume 
	Sound.Parent = parent 
	return Sound 
end
local Sound = createSound("rbxassetid://5268594777")
Sound.Looped = false 
Sound:Play()
wait(5)  
local Creator = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors%20Entity%20Spawner/Source.lua"))()

-- Create entity

local entity = Creator.createEntity({
    CustomName = "Savage Smile", -- Custom name of your entity
    Model = "rbxassetid://12645075460", -- Can be GitHub file or rbxassetid
    Speed = 100, -- Percentage, 100 = default Rush speed
    DelayTime = 3, -- Time before starting cycles (seconds)
    HeightOffset = 0,
    CanKill = true,
    BreakLights = false,
    KillRange = 40,
    BackwardsMovement = true,
    FlickerLights = {
        true, -- Enabled
        2.5, -- Time (seconds)
    },
    Cycles = {
        Min = 3,
        Max = 3
    },
    CamShake = {
        true, -- Enabled
        {5, 15, 0.1, 1}, -- Shake values (don't change if you don't know)
        100, -- Shake start distance (from Entity to you)
    },
    Jumpscare = {
        true, -- Enabled ('false' if you don't want jumpscare)
        {
            Image1 = "https://tr.rbxcdn.com/1e7945796277100aec5f98858407b523/420/420/Decal/Png", -- Image1 url
            Image2 = "https://tr.rbxcdn.com/1e7945796277100aec5f98858407b523/420/420/Decal/Png", -- Image2 url
            Shake = false,
            Sound1 = {
                5351101493, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Sound2 = {
                5351101493, -- SoundId
                { Volume = 5 }, -- Sound properties
            },
            Flashing = {
                true, -- Enabled
                Color3.fromRGB(6, 38, 135), -- Color
            },
            Tease = {
                false, -- Enabled ('false' if you don't want tease)
                Min = 1,
                Max = 3,
            },
        },
    },
    CustomDialog = {"Bạn Đã Chết Bởi Thực Thể Nào Đó Gọi là Savage Smile", "Hãy Chủng Bị Tinh Thần Trước Khi Nghe Tiếng La Hét Của Nó", "E̸̩̠̠͙̜͓͔͔̮̾R̴̨͈̙̣̙̟̼̹͇͊̊̔̈́͂͗͒̊̋́̔͗̑̀͝R̵̡̧͍̱̥̤̞͓̗̰̩͗̓͒̿̊͋̒͝Ǫ̵̦̳̰̻̹̳̖̦͇̝̅̅̀͂͊̊̈́̏̈̽̓͝R̵̛͕̙͉̥̤͕̹̪̄́̈́̽́̓̈́́̈́̍͌", "..."}, -- Custom death message (can be as long as you want)
})

-----[[ Advanced ]]-----

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity:", entity, "has entered room:", room)
    Speed = 0
    wait(2)
    Speed = 170
end

entity.Debug.OnEntitySpawned = function(entityModel)
    print("Entity has spawned:", entityModel)
local bruh = workspace.Rebound.Torso
local TweenService = game:GetService("TweenService")
local spawn = Instance.new("Sound")
spawn.Parent = bruh
spawn.Name = "ReboundSpawn"
spawn.SoundId = "rbxassetid://9114221327"
spawn.Volume = 5
spawn.RollOffMaxDistance = 10000
spawn.RollOffMinDistance = 450
spawn:Play()
    local move = GetGitSound("https://github.com/check78/worldcuuuup/blob/main/DoomBegin.mp3?raw=true","Reboun")
    move.Parent = bruh
    move.Name = "ReboundMoving"
    move.Volume = 0
    move.Looped = true
local vroom = TweenService:Create(move, TweenInfo.new(2),{Volume = 0.2})
    local distort = Instance.new("DistortionSoundEffect")
    distort.Level = 0.75
    distort.Parent = move
	move.RollOffMaxDistance = 200
	move.RollOffMinDistance = 100
    local tree = Instance.new("TremoloSoundEffect")
    tree.Depth = 1
    tree.Duty = 1
    tree.Frequency = 5
    tree.Parent = move
    local eq = Instance.new("EqualizerSoundEffect")
    eq.HighGain = -60
    eq.MidGain = 10
    eq.LowGain = 10
    eq.Parent = move
	wait(1)
    vroom:Play()
	move:Play()
end

entity.Debug.OnEntityDespawned = function(entityModel)
    print("Entity has despawned:", entityModel)
end

entity.Debug.OnEntityStartMoving = function(entityModel)
    print("Entity has started moving:", entityModel)
end

entity.Debug.OnEntityFinishedRebound = function(entityModel)
    print("Entity finished rebound:", entityModel)
	
end

entity.Debug.OnDeath = function()
    warn("You died.")
local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(15),{TintColor = Color3.fromRGB(255, 0, 0)})
TW:Play()
    firesignal(game.ReplicatedStorage.EntityInfo.DeathHint.OnClientEvent, {"Bạn Đã Chết Bởi Thực Thể Nào Đó Gọi là Savage Smile", "Hãy Chủng Bị Tinh Thần Trước Khi Nghe Tiếng La Hét Của Nó", "E̸̩̠̠͙̜͓͔͔̮̾R̴̨͈̙̣̙̟̼̹͇͊̊̔̈́͂͗͒̊̋́̔͗̑̀͝R̵̡̧͍̱̥̤̞͓̗̰̩͗̓͒̿̊͋̒͝Ǫ̵̦̳̰̻̹̳̖̦͇̝̅̅̀͂͊̊̈́̏̈̽̓͝R̵̛͕̙͉̥̤͕̹̪̄́̈́̽́̓̈́́̈́̍͌", "..."}, "Blue")

	local ReSt = game.ReplicatedStorage
	local Plr = game.Players.LocalPlayer

		scare:Play()
	    ReSt.GameStats["Player_".. Plr.Name].Total.DeathCause.Value = "Rebound"
		wait(0.5)
		scare:Destroy()
end

------------------------

-- Run the created entity
Creator.runEntity(entity)
-- entities 2
local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Entity%20Spawner/Source.lua"))()

---====== Create entity ======---

local entity = Spawner.createEntity({
    CustomName = "A - 666",
    Model = "11586985083", -- Your entity's model url here ("rbxassetid://1234567890" or GitHub raw url)
    Speed = 100,
    MoveDelay = 2,
    HeightOffset = 0,
    CanKill = true,
    KillRange = 90,
    SpawnInFront = true,
    ShatterLights = false,
    FlickerLights = {
        Enabled = false,
        Duration = 1
    },
    Cycles = {
        Min = 1,
        Max = 4,
        Delay = 2
    },
    CamShake = {
        Enabled = true,
        Values = {1.5, 20, 0.1, 1},
        Range = 85
    },
    ResistCrucifix = true,
    BreakCrucifix = true,
    DeathMessage = {"Hm.. Hello.", "You're back here again.", "Well Who'd you die to?", "Oh. The squiggly-face.", "It starts attacking after Savage Smuke.", "You can do this. I believe in you.", "I'll see you next time right?"},
    IsCuriousLight = true
})



Spawner.runEntity(entity)
function createSound(id, volume, parent)
	assert(id, "sound id wasn't given")
	id = tostring(id):gsub("%D", "") --only keeping the numbers
	assert(id ~= "", "sound id isn't a number") 
	volume = volume or 10 --0.5 is the default volume
	parent = parent or workspace --workspace is the default parent
	local Sound = Instance.new("Sound") 
	Sound.SoundId = "rbxassetid://"..id 
	Sound.Volume = volume 
	Sound.Parent = parent 
	return Sound 
end
local Sound = createSound("rbxassetid://5408539313")
Sound.Looped = false 
Sound:Play() 
game.Lighting.MainColorCorrection.TintColor = Color3.fromRGB(0, 0, 0)
game.Lighting.MainColorCorrection.Contrast = 0.2
game.Lighting.MainColorCorrection.Saturation = -0.7
local tween = game:GetService("TweenService")
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Contrast = 0}):Play()
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Saturation = 0}):Play()
local TweenService = game:GetService("TweenService")
local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5),{TintColor = Color3.fromRGB(0, 0, 255)})
TW:Play()
-- Achievement
local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()
Achievements.Get({
    Title = "Ồn Quá Đi",
    Desc = "Sóng Sót Khỏi Thực Thể Savage Smile",
    Reason = "Savage Smile",
    Image = "https://tr.rbxcdn.com/aa34897fa40add1a6f9855d995ebff12/420/420/Model/Png",
})
-- bruh
local Creator = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors%20Entity%20Spawner/Source.lua"))()

-- Create entity

local entity = Creator.createEntity({
    CustomName = "A-50", -- Custom name of your entity
    Model = "rbxassetid://11470738519", -- Can be GitHub file or rbxassetid
    Speed = 100, -- Percentage, 100 = default Rush speed
    DelayTime = 3, -- Time before starting cycles (seconds)
    HeightOffset = 0,
    CanKill = true,
    BreakLights = false,
    KillRange = 40,
    BackwardsMovement = false,
    FlickerLights = {
        false, -- Enabled
        2.5, -- Time (seconds)
    },
    Cycles = {
        Min = 1,
        Max = 1
    },
    CamShake = {
        true, -- Enabled
        {5, 15, 0.1, 1}, -- Shake values (don't change if you don't know)
        100, -- Shake start distance (from Entity to you)
    },
    Jumpscare = {
        false, -- Enabled ('false' if you don't want jumpscare)
        {
            Image1 = "https://tr.rbxcdn.com/1e7945796277100aec5f98858407b523/420/420/Decal/Png", -- Image1 url
            Image2 = "https://tr.rbxcdn.com/1e7945796277100aec5f98858407b523/420/420/Decal/Png", -- Image2 url
            Shake = false,
            Sound1 = {
                5351101493, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Sound2 = {
                5351101493, -- SoundId
                { Volume = 5 }, -- Sound properties
            },
            Flashing = {
                true, -- Enabled
                Color3.fromRGB(6, 38, 135), -- Color
            },
            Tease = {
                false, -- Enabled ('false' if you don't want tease)
                Min = 1,
                Max = 3,
            },
        },
    },
    CustomDialog = {"..."}, -- Custom death message (can be as long as you want)
})

-----[[ Advanced ]]-----

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity:", entity, "has entered room:", room)
    Speed = 0
    wait(2)
    Speed = 170
end

entity.Debug.OnEntitySpawned = function(entityModel)
    print("Entity has spawned:", entityModel)
local bruh = workspace.Rebound.Torso
local TweenService = game:GetService("TweenService")
local spawn = Instance.new("Sound")
spawn.Parent = bruh
spawn.Name = "ReboundSpawn"
spawn.SoundId = "rbxassetid://9114221327"
spawn.Volume = 5
spawn.RollOffMaxDistance = 10000
spawn.RollOffMinDistance = 450
spawn:Play()
    local move = GetGitSound("https://github.com/check78/worldcuuuup/blob/main/DoomBegin.mp3?raw=true","Reboun")
    move.Parent = bruh
    move.Name = "ReboundMoving"
    move.Volume = 0
    move.Looped = true
local vroom = TweenService:Create(move, TweenInfo.new(2),{Volume = 0.2})
    local distort = Instance.new("DistortionSoundEffect")
    distort.Level = 0.75
    distort.Parent = move
	move.RollOffMaxDistance = 200
	move.RollOffMinDistance = 100
    local tree = Instance.new("TremoloSoundEffect")
    tree.Depth = 1
    tree.Duty = 1
    tree.Frequency = 5
    tree.Parent = move
    local eq = Instance.new("EqualizerSoundEffect")
    eq.HighGain = -60
    eq.MidGain = 10
    eq.LowGain = 10
    eq.Parent = move
	wait(1)
    vroom:Play()
	move:Play()
end

entity.Debug.OnEntityDespawned = function(entityModel)
    print("Entity has despawned:", entityModel)
end

entity.Debug.OnEntityStartMoving = function(entityModel)
    print("Entity has started moving:", entityModel)
end

entity.Debug.OnEntityFinishedRebound = function(entityModel)
    print("Entity finished rebound:", entityModel)
	
end

entity.Debug.OnDeath = function()
    warn("You died.")
local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(15),{TintColor = Color3.fromRGB(255, 0, 0)})
TW:Play()
    firesignal(game.ReplicatedStorage.EntityInfo.DeathHint.OnClientEvent, {"..."}, "Blue")

	local ReSt = game.ReplicatedStorage
	local Plr = game.Players.LocalPlayer

		scare:Play()
	    ReSt.GameStats["Player_".. Plr.Name].Total.DeathCause.Value = "Rebound"
		wait(0.5)
		scare:Destroy()
end

------------------------

-- Run the created entity
Creator.runEntity(entity)
function createSound(id, volume, parent)
	assert(id, "sound id wasn't given")
	id = tostring(id):gsub("%D", "") --only keeping the numbers
	assert(id ~= "", "sound id isn't a number") 
	volume = volume or 0.5 --0.5 is the default volume
	parent = parent or workspace --workspace is the default parent
	local Sound = Instance.new("Sound") 
	Sound.SoundId = "rbxassetid://"..id 
	Sound.Volume = volume 
	Sound.Parent = parent 
	return Sound 
end
local Sound = createSound("rbxassetid://9120829080")
Sound.Looped = false 
Sound:Play() 
local CameraShaker = require(game.ReplicatedStorage.CameraShaker)
local camara = game.Workspace.CurrentCamera
local camShake = CameraShaker.new(Enum.RenderPriority.Camera.Value, function(shakeCf)
	camara.CFrame = camara.CFrame * shakeCf
end)
camShake:Start()
camShake:ShakeOnce(15,8.8,0,2,1,6)
-- REBOUND --
wait(85)
game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
local part = Instance.new("Part")
part.Name = "Bound"
part.Parent = workspace
game.Lighting.MainColorCorrection.TintColor = Color3.fromRGB(61, 171, 98)
game.Lighting.MainColorCorrection.Contrast = 0.2
game.Lighting.MainColorCorrection.Saturation = -0.7
local tween = game:GetService("TweenService")
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Contrast = 0}):Play()
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Saturation = 0}):Play()
local TweenService = game:GetService("TweenService")
local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5),{TintColor = Color3.fromRGB(0, 0, 255)})
TW:Play()
local cue1 = Instance.new("Sound")
cue1.Parent = game.Workspace
cue1.Name = "Scream"
cue1.SoundId = "rbxassetid://9114397505"
local distort = Instance.new("DistortionSoundEffect")
distort.Parent = cue1
distort.Level = 1
local distort2 = Instance.new("DistortionSoundEffect")
distort2.Parent = cue1
distort2.Level = 1
local pitch = Instance.new("PitchShiftSoundEffect")
pitch.Parent = cue1
pitch.Octave = 0.5
local pitch2 = Instance.new("PitchShiftSoundEffect")
pitch2.Parent = cue1
pitch2.Octave = 0.5
local pitch3 = Instance.new("PitchShiftSoundEffect")
pitch3.Parent = cue1
pitch3.Octave = 0.5
cue1.Volume = 0.1
cue1:Play()
local cue2 = Instance.new("Sound")
cue2.Parent = game.Workspace
cue2.Name = "Spawn"
cue2.SoundId = "rbxassetid://9114221327"
cue2.Volume = 3
cue2:Play()
local CameraShaker = require(game.ReplicatedStorage.CameraShaker)
local camara = game.Workspace.CurrentCamera
local camShake = CameraShaker.new(Enum.RenderPriority.Camera.Value, function(shakeCf)
	camara.CFrame = camara.CFrame * shakeCf
end)
camShake:Start()
camShake:ShakeOnce(10,3,0.1,6,2,0.5)
wait(2.8)

function GetGitSound(GithubSnd,SoundName)
	local url=GithubSnd
	if not isfile(SoundName..".mp3") then
		writefile(SoundName..".mp3", game:HttpGet(url))
	end
	local sound=Instance.new("Sound")
	sound.SoundId=(getcustomasset or getsynasset)(SoundName..".mp3")
	return sound
end

local scare = Instance.new("Sound")
scare.Parent = game.Workspace
scare.Name = "MyEarsBurn"
scare.SoundId = "rbxassetid://5567523008"
scare.PlaybackSpeed = 3
scare.Volume = 1

local shift = Instance.new("PitchShiftSoundEffect")
shift.Octave = 0.5
shift.Parent = scare

local distort = Instance.new("DistortionSoundEffect")
distort.Parent = scare
distort.Level = 1

local TweenService = game:GetService("TweenService")
local spookee = TweenService:Create(scare, TweenInfo.new(0.3),{Volume = 0})

local Creator = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors%20Entity%20Spawner/Source.lua"))()

-- Create entity

local entity = Creator.createEntity({
    CustomName = "Rebound", -- Custom name of your entity
    Model = "rbxassetid://12403179451", -- Can be GitHub file or rbxassetid
    Speed = 100, -- Percentage, 100 = default Rush speed
    DelayTime = 1.4, -- Time before starting cycles (seconds)
    HeightOffset = 0,
    CanKill = true,
    BreakLights = false,
    KillRange = 40,
    BackwardsMovement = true,
    FlickerLights = {
        false, -- Enabled
        2.5, -- Time (seconds)
    },
    Cycles = {
        Min = 1,
        Max = 1
    },
    CamShake = {
        true, -- Enabled
        {5, 15, 0.1, 1}, -- Shake values (don't change if you don't know)
        100, -- Shake start distance (from Entity to you)
    },
    Jumpscare = {
        false, -- Enabled ('false' if you don't want jumpscare)
        {
            Image1 = "https://www.roblox.com/library/11862656491", -- Image1 url
            Image2 = "https://www.roblox.com/library/11862656491", -- Image2 url
            Shake = false,
            Sound1 = {
                0, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Sound2 = {
                5567523008, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Flashing = {
                true, -- Enabled
                Color3.fromRGB(6, 38, 135), -- Color
            },
            Tease = {
                false, -- Enabled ('false' if you don't want tease)
                Min = 1,
                Max = 3,
            },
        },
    },
    CustomDialog = {"Bạn Đã Chết Bởi Rebound...", "Nó xuất hiện sẽ có tiếng rất lớn phát ra", "Bạn không thể trốn ở những nơi khác ngoài tủ vì chúng rất nhạy", "vì vậy hãy trốn đến khi nào bạn cảm thấy an toàn"}, -- Custom death message (can be as long as you want)
})

-----[[ Advanced ]]-----

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity:", entity, "has entered room:", room)
    Speed = 0
    wait(2)
    Speed = 170
end

entity.Debug.OnEntitySpawned = function(entityModel)
    print("Entity has spawned:", entityModel)
workspace.Rebound.Rebound_Cue:Destroy()
workspace.Rebound.Rebound_Cue2:Destroy()
workspace.Rebound.Torso.Idle:Destroy()
workspace.Rebound.Torso.Footsteps:Destroy()
local bruh = workspace.Rebound.Torso
local TweenService = game:GetService("TweenService")
local spawn = Instance.new("Sound")
spawn.Parent = bruh
spawn.Name = "ReboundSpawn"
spawn.SoundId = "rbxassetid://9114221327"
spawn.Volume = 5
spawn.RollOffMaxDistance = 10000
spawn.RollOffMinDistance = 450
spawn:Play()
    local move = GetGitSound("https://github.com/check78/worldcuuuup/blob/main/DoomBegin.mp3?raw=true","Reboun")
    move.Parent = bruh
    move.Name = "ReboundMoving"
    move.Volume = 0
    move.Looped = true
local vroom = TweenService:Create(move, TweenInfo.new(2),{Volume = 0.2})
    local distort = Instance.new("DistortionSoundEffect")
    distort.Level = 0.75
    distort.Parent = move
	move.RollOffMaxDistance = 200
	move.RollOffMinDistance = 100
    local tree = Instance.new("TremoloSoundEffect")
    tree.Depth = 1
    tree.Duty = 1
    tree.Frequency = 5
    tree.Parent = move
    local eq = Instance.new("EqualizerSoundEffect")
    eq.HighGain = -60
    eq.MidGain = 10
    eq.LowGain = 10
    eq.Parent = move
	wait(1)
    vroom:Play()
	move:Play()
end

entity.Debug.OnEntityDespawned = function(entityModel)
    print("Entity has despawned:", entityModel)
end

entity.Debug.OnEntityStartMoving = function(entityModel)
    print("Entity has started moving:", entityModel)
end

entity.Debug.OnEntityFinishedRebound = function(entityModel)
    print("Entity finished rebound:", entityModel)
end

entity.Debug.OnDeath = function()
    warn("You died.")
local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(15),{TintColor = Color3.fromRGB(255, 0, 0)})
TW:Play()
    firesignal(game.ReplicatedStorage.EntityInfo.DeathHint.OnClientEvent, {"Bạn Đã Chết Bởi Rebound...", "Nó xuất hiện sẽ có tiếng rất lớn phát ra", "Bạn không thể trốn ở những nơi khác ngoài tủ vì chúng rất nhạy", "vì vậy hãy trốn đến khi nào bạn cảm thấy an toàn"}, "Blue")

	local ReSt = game.ReplicatedStorage
	local Plr = game.Players.LocalPlayer

		scare:Play()
	    ReSt.GameStats["Player_".. Plr.Name].Total.DeathCause.Value = "Rebound"
		wait(0.5)
		scare:Destroy()

end

------------------------

-- Run the created entity
Creator.runEntity(entity)
game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
wait(0.5)
function createSound(id, volume, parent)
	assert(id, "sound id wasn't given")
	id = tostring(id):gsub("%D", "") --only keeping the numbers
	assert(id ~= "", "sound id isn't a number") 
	volume = volume or 2 --0.5 is the default volume
	parent = parent or workspace --workspace is the default parent
	local Sound = Instance.new("Sound") 
	Sound.SoundId = "rbxassetid://"..id 
	Sound.Volume = volume 
	Sound.Parent = parent 
	return Sound 
end
local Sound = createSound("rbxassetid://4398878054")
Sound.Looped = false 
Sound:Play() 
game.Lighting.MainColorCorrection.TintColor = Color3.fromRGB(61, 171, 98)
game.Lighting.MainColorCorrection.Contrast = 0.2
game.Lighting.MainColorCorrection.Saturation = -0.7
local tween = game:GetService("TweenService")
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Contrast = 0}):Play()
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Saturation = 0}):Play()
local TweenService = game:GetService("TweenService")
local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5),{TintColor = Color3.fromRGB(0, 0, 255)})
TW:Play()
local randomnumber = math.random(3,5)
print(randomnumber)

for i = 3,randomnumber do
function GetGitSound(GithubSnd,SoundName)
	local url=GithubSnd
	if not isfile(SoundName..".mp3") then
		writefile(SoundName..".mp3", game:HttpGet(url))
	end
	local sound=Instance.new("Sound")
	sound.SoundId=(getcustomasset or getsynasset)(SoundName..".mp3")
	return sound
end

local scare = Instance.new("Sound")
scare.Parent = game.Workspace
scare.Name = "MyEarsBurn"
scare.SoundId = "rbxassetid://5567523008"
scare.PlaybackSpeed = 3
scare.Volume = 1

local shift = Instance.new("PitchShiftSoundEffect")
shift.Octave = 0.5
shift.Parent = scare

local distort = Instance.new("DistortionSoundEffect")
distort.Parent = scare
distort.Level = 1

local TweenService = game:GetService("TweenService")

local Creator = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors%20Entity%20Spawner/Source.lua"))()

-- Create entity

local entity = Creator.createEntity({
    CustomName = "Rebound", -- Custom name of your entity
    Model = "rbxassetid://12403179451", -- Can be GitHub file or rbxassetid
    Speed = 100, -- Percentage, 100 = default Rush speed
    DelayTime = 1.4, -- Time before starting cycles (seconds)
    HeightOffset = 0,
    CanKill = true,
    BreakLights = false,
    KillRange = 40,
    BackwardsMovement = true,
    FlickerLights = {
        false, -- Enabled
        2.5, -- Time (seconds)
    },
    Cycles = {
        Min = 1,
        Max = 1
    },
    CamShake = {
        true, -- Enabled
        {5, 15, 0.1, 1}, -- Shake values (don't change if you don't know)
        100, -- Shake start distance (from Entity to you)
    },
    Jumpscare = {
        false, -- Enabled ('false' if you don't want jumpscare)
        {
            Image1 = "https://www.roblox.com/library/11862656491", -- Image1 url
            Image2 = "https://www.roblox.com/library/11862656491", -- Image2 url
            Shake = false,
            Sound1 = {
                0, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Sound2 = {
                5567523008, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Flashing = {
                true, -- Enabled
                Color3.fromRGB(6, 38, 135), -- Color
            },
            Tease = {
                false, -- Enabled ('false' if you don't want tease)
                Min = 1,
                Max = 3,
            },
        },
    },
    CustomDialog = {"Bạn Đã Chết Bởi Rebound...", "Nó xuất hiện sẽ có tiếng rất lớn phát ra", "Bạn không thể trốn ở những nơi khác ngoài tủ vì chúng rất nhạy", "vì vậy hãy trốn đến khi nào bạn cảm thấy an toàn"}, -- Custom death message (can be as long as you want)
})

-----[[ Advanced ]]-----

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity:", entity, "has entered room:", room)
    Speed = 0
    wait(2)
    Speed = 170
end

entity.Debug.OnEntitySpawned = function(entityModel)
    print("Entity has spawned:", entityModel)
workspace.Rebound.Rebound_Cue:Destroy()
workspace.Rebound.Rebound_Cue2:Destroy()
workspace.Rebound.Torso.Idle:Destroy()
workspace.Rebound.Torso.Footsteps:Destroy()
local bruh = workspace.Rebound.Torso
local TweenService = game:GetService("TweenService")
local spawn = Instance.new("Sound")
spawn.Parent = bruh
spawn.Name = "ReboundSpawn"
spawn.SoundId = "rbxassetid://9114221327"
spawn.Volume = 5
spawn.RollOffMaxDistance = 10000
spawn.RollOffMinDistance = 450
spawn:Play()
    local move = GetGitSound("https://github.com/check78/worldcuuuup/blob/main/DoomBegin.mp3?raw=true","Reboun")
    move.Parent = bruh
    move.Name = "ReboundMoving"
    move.Volume = 0
    move.Looped = true
local vroom = TweenService:Create(move, TweenInfo.new(2),{Volume = 0.2})
    local distort = Instance.new("DistortionSoundEffect")
    distort.Level = 0.75
    distort.Parent = move
	move.RollOffMaxDistance = 200
	move.RollOffMinDistance = 100
    local tree = Instance.new("TremoloSoundEffect")
    tree.Depth = 1
    tree.Duty = 1
    tree.Frequency = 5
    tree.Parent = move
    local eq = Instance.new("EqualizerSoundEffect")
    eq.HighGain = -60
    eq.MidGain = 10
    eq.LowGain = 10
    eq.Parent = move
	wait(1)
    vroom:Play()
	move:Play()
end

entity.Debug.OnEntityDespawned = function(entityModel)
    print("Entity has despawned:", entityModel)
end

entity.Debug.OnEntityStartMoving = function(entityModel)
    print("Entity has started moving:", entityModel)
end

entity.Debug.OnEntityFinishedRebound = function(entityModel)
    print("Entity finished rebound:", entityModel)
end

entity.Debug.OnDeath = function()
    warn("You died.")
local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(15),{TintColor = Color3.fromRGB(255, 0, 0)})
TW:Play()
    firesignal(game.ReplicatedStorage.EntityInfo.DeathHint.OnClientEvent, {"Bạn Đã Chết Bởi Rebound...", "Nó xuất hiện sẽ có tiếng rất lớn phát ra", "Bạn không thể trốn ở những nơi khác ngoài tủ vì chúng rất nhạy", "vì vậy hãy trốn đến khi nào bạn cảm thấy an toàn"}, "Blue")

	local ReSt = game.ReplicatedStorage
	local Plr = game.Players.LocalPlayer

		scare:Play()
	    ReSt.GameStats["Player_".. Plr.Name].Total.DeathCause.Value = "Rebound"
		wait(0.5)
		scare:Destroy()

end

------------------------

-- Run the created entity
Creator.runEntity(entity)
end
print("Laps ended. Done.")
part:Destroy()
-- Achievement
local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()
Achievements.Get({
    Title = "Sẽ Quay Lại Sớm Thôi",
    Desc = "Sóng Sót Khỏi Thực Thể Rebound",
    Reason = "Innut Rebound",
    Image = "https://static.wikia.nocookie.net/endless-doors-roblox/images/d/d0/Rebound.png/revision/latest?cb=20221005130101",
})
-- Keep Up
local Creator = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors%20Entity%20Spawner/Source.lua"))()

-- Create entity

local entity = Creator.createEntity({
    CustomName = "A-50", -- Custom name of your entity
    Model = "rbxassetid://11470738519", -- Can be GitHub file or rbxassetid
    Speed = 100, -- Percentage, 100 = default Rush speed
    DelayTime = 3, -- Time before starting cycles (seconds)
    HeightOffset = 0,
    CanKill = true,
    BreakLights = false,
    KillRange = 40,
    BackwardsMovement = false,
    FlickerLights = {
        false, -- Enabled
        2.5, -- Time (seconds)
    },
    Cycles = {
        Min = 1,
        Max = 1
    },
    CamShake = {
        true, -- Enabled
        {5, 15, 0.1, 1}, -- Shake values (don't change if you don't know)
        100, -- Shake start distance (from Entity to you)
    },
    Jumpscare = {
        false, -- Enabled ('false' if you don't want jumpscare)
        {
            Image1 = "https://tr.rbxcdn.com/1e7945796277100aec5f98858407b523/420/420/Decal/Png", -- Image1 url
            Image2 = "https://tr.rbxcdn.com/1e7945796277100aec5f98858407b523/420/420/Decal/Png", -- Image2 url
            Shake = false,
            Sound1 = {
                5351101493, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Sound2 = {
                5351101493, -- SoundId
                { Volume = 5 }, -- Sound properties
            },
            Flashing = {
                true, -- Enabled
                Color3.fromRGB(6, 38, 135), -- Color
            },
            Tease = {
                false, -- Enabled ('false' if you don't want tease)
                Min = 1,
                Max = 3,
            },
        },
    },
    CustomDialog = {"..."}, -- Custom death message (can be as long as you want)
})

-----[[ Advanced ]]-----

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity:", entity, "has entered room:", room)
    Speed = 0
    wait(2)
    Speed = 170
end

entity.Debug.OnEntitySpawned = function(entityModel)
    print("Entity has spawned:", entityModel)
local bruh = workspace.Rebound.Torso
local TweenService = game:GetService("TweenService")
local spawn = Instance.new("Sound")
spawn.Parent = bruh
spawn.Name = "ReboundSpawn"
spawn.SoundId = "rbxassetid://9114221327"
spawn.Volume = 5
spawn.RollOffMaxDistance = 10000
spawn.RollOffMinDistance = 450
spawn:Play()
    local move = GetGitSound("https://github.com/check78/worldcuuuup/blob/main/DoomBegin.mp3?raw=true","Reboun")
    move.Parent = bruh
    move.Name = "ReboundMoving"
    move.Volume = 0
    move.Looped = true
local vroom = TweenService:Create(move, TweenInfo.new(2),{Volume = 0.2})
    local distort = Instance.new("DistortionSoundEffect")
    distort.Level = 0.75
    distort.Parent = move
	move.RollOffMaxDistance = 200
	move.RollOffMinDistance = 100
    local tree = Instance.new("TremoloSoundEffect")
    tree.Depth = 1
    tree.Duty = 1
    tree.Frequency = 5
    tree.Parent = move
    local eq = Instance.new("EqualizerSoundEffect")
    eq.HighGain = -60
    eq.MidGain = 10
    eq.LowGain = 10
    eq.Parent = move
	wait(1)
    vroom:Play()
	move:Play()
end

entity.Debug.OnEntityDespawned = function(entityModel)
    print("Entity has despawned:", entityModel)
end

entity.Debug.OnEntityStartMoving = function(entityModel)
    print("Entity has started moving:", entityModel)
end

entity.Debug.OnEntityFinishedRebound = function(entityModel)
    print("Entity finished rebound:", entityModel)
	
end

entity.Debug.OnDeath = function()
    warn("You died.")
local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(15),{TintColor = Color3.fromRGB(255, 0, 0)})
TW:Play()
    firesignal(game.ReplicatedStorage.EntityInfo.DeathHint.OnClientEvent, {"..."}, "Blue")

	local ReSt = game.ReplicatedStorage
	local Plr = game.Players.LocalPlayer

		scare:Play()
	    ReSt.GameStats["Player_".. Plr.Name].Total.DeathCause.Value = "Rebound"
		wait(0.5)
		scare:Destroy()
end

------------------------

-- Run the created entity
Creator.runEntity(entity)
function createSound(id, volume, parent)
	assert(id, "sound id wasn't given")
	id = tostring(id):gsub("%D", "") --only keeping the numbers
	assert(id ~= "", "sound id isn't a number") 
	volume = volume or 0.5 --0.5 is the default volume
	parent = parent or workspace --workspace is the default parent
	local Sound = Instance.new("Sound") 
	Sound.SoundId = "rbxassetid://"..id 
	Sound.Volume = volume 
	Sound.Parent = parent 
	return Sound 
end
local Sound = createSound("rbxassetid://9120829080")
Sound.Looped = false 
Sound:Play() 
local CameraShaker = require(game.ReplicatedStorage.CameraShaker)
local camara = game.Workspace.CurrentCamera
local camShake = CameraShaker.new(Enum.RenderPriority.Camera.Value, function(shakeCf)
	camara.CFrame = camara.CFrame * shakeCf
end)
camShake:Start()
camShake:ShakeOnce(15,8.8,0,2,1,6)
-- A-120
wait(30)
game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
workspace.AllRooms[game.ReplicatedStorage.GameData.LatestRoom.Value].Assets.Light_Fixtures:Destroy()
--[[
	WARNING: Heads up! This script has not been verified by ScriptBlox. Use at your own risk!
]]



local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Entity%20Spawner/Source.lua"))()

---====== Create entity ======---

local entity = Spawner.createEntity({
    CustomName = "A - 120",
    Model = "https://github.com/tonyBflako/vynixusdoors/blob/main/A120.rbxm?raw=true", -- Your entity's model url here ("rbxassetid://1234567890" or GitHub raw url)
    Speed = 60,
    MoveDelay = 2,
    HeightOffset = 0,
    CanKill = true,
    KillRange = 90,
    SpawnInFront = true,
    ShatterLights = false,
    FlickerLights = {
        Enabled = false,
        Duration = 1
    },
    Cycles = {
        Min = 1,
        Max = 3,
        Delay = 2
    },
    CamShake = {
        Enabled = true,
        Values = {1.5, 20, 0.1, 1},
        Range = 85
    },
    ResistCrucifix = true,
    BreakCrucifix = true,
    DeathMessage = {"Hm.. Hello.", "You're back here again.", "Well Who'd you die to?", "Oh. The squiggly-face.", "It starts attacking after A-120.", "You can do this. I believe in you.", "I'll see you next time right?"},
    IsCuriousLight = true
})



Spawner.runEntity(entity)
----------------------
-- Blood Eyes
wait(30)
game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
loadstring(game:HttpGet("https://raw.githubusercontent.com/idontknowwhatmyname/blank/main/BloodEyes"))()
-- Achievement
local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()
Achievements.Get({
    Title = "Thật Là Nhiều Máu",
    Desc = "Sống Sót Khỏi Blood Eyes",
    Reason = "Blood Eyes",
    Image = "https://i.ibb.co/VY7W97H/image-2023-05-03-172833042-removebg-preview.png",
})
----------------------
wait(55)
-- The Creepy Teeth (rare entity)
-- Create entity

wait(1.5)
function createSound(id, volume, parent)
	assert(id, "sound id wasn't given")
	id = tostring(id):gsub("%D", "") --only keeping the numbers
	assert(id ~= "", "sound id isn't a number") 
	volume = volume or 2 --0.5 is the default volume
	parent = parent or workspace --workspace is the default parent
	local Sound = Instance.new("Sound") 
	Sound.SoundId = "rbxassetid://"..id 
	Sound.Volume = volume 
	Sound.Parent = parent 
	return Sound 
end
local Sound = createSound("rbxassetid://4398878054")
Sound.Looped = false 
Sound:Play() 
game.Lighting.MainColorCorrection.TintColor = Color3.fromRGB(255, 0, 0)
game.Lighting.MainColorCorrection.Contrast = 0.2
game.Lighting.MainColorCorrection.Saturation = -0.7
local tween = game:GetService("TweenService")
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Contrast = 0}):Play()
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Saturation = 0}):Play()
local TweenService = game:GetService("TweenService")
local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5),{TintColor = Color3.fromRGB(0, 0, 255)})
TW:Play()
wait(3)
function createSound(id, volume, parent)
	assert(id, "sound id wasn't given")
	id = tostring(id):gsub("%D", "") --only keeping the numbers
	assert(id ~= "", "sound id isn't a number") 
	volume = volume or 2 --0.5 is the default volume
	parent = parent or workspace --workspace is the default parent
	local Sound = Instance.new("Sound") 
	Sound.SoundId = "rbxassetid://"..id 
	Sound.Volume = volume 
	Sound.Parent = parent 
	return Sound 
end
local Sound = createSound("rbxassetid://4398878054")
Sound.Looped = false 
Sound:Play() 
game.Lighting.MainColorCorrection.TintColor = Color3.fromRGB(255, 0, 0)
game.Lighting.MainColorCorrection.Contrast = 0.2
game.Lighting.MainColorCorrection.Saturation = -0.7
local tween = game:GetService("TweenService")
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Contrast = 0}):Play()
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Saturation = 0}):Play()
local TweenService = game:GetService("TweenService")
local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5),{TintColor = Color3.fromRGB(0, 0, 255)})
TW:Play()
wait(6)
game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
function createSound(id, volume, parent)
	assert(id, "sound id wasn't given")
	id = tostring(id):gsub("%D", "") --only keeping the numbers
	assert(id ~= "", "sound id isn't a number") 
	volume = volume or 2 --0.5 is the default volume
	parent = parent or workspace --workspace is the default parent
	local Sound = Instance.new("Sound") 
	Sound.SoundId = "rbxassetid://"..id 
	Sound.Volume = volume 
	Sound.Parent = parent 
	return Sound 
end
local Sound = createSound("rbxassetid://4398878054")
Sound.Looped = false 
Sound:Play() 
game.Lighting.MainColorCorrection.TintColor = Color3.fromRGB(255, 0, 0)
game.Lighting.MainColorCorrection.Contrast = 0.2
game.Lighting.MainColorCorrection.Saturation = -0.7
local tween = game:GetService("TweenService")
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Contrast = 0}):Play()
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Saturation = 0}):Play()
local TweenService = game:GetService("TweenService")
local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5),{TintColor = Color3.fromRGB(0, 0, 255)})
TW:Play()
wait(8)
function createSound(id, volume, parent)
	assert(id, "sound id wasn't given")
	id = tostring(id):gsub("%D", "") --only keeping the numbers
	assert(id ~= "", "sound id isn't a number") 
	volume = volume or 2 --0.5 is the default volume
	parent = parent or workspace --workspace is the default parent
	local Sound = Instance.new("Sound") 
	Sound.SoundId = "rbxassetid://"..id 
	Sound.Volume = volume 
	Sound.Parent = parent 
	return Sound 
end
local Sound = createSound("rbxassetid://4398878054")
Sound.Looped = false 
Sound:Play() 
game.Lighting.MainColorCorrection.TintColor = Color3.fromRGB(255, 0, 0)
game.Lighting.MainColorCorrection.Contrast = 0.2
game.Lighting.MainColorCorrection.Saturation = -0.7
local tween = game:GetService("TweenService")
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Contrast = 0}):Play()
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Saturation = 0}):Play()
local TweenService = game:GetService("TweenService")
local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5),{TintColor = Color3.fromRGB(0, 0, 255)})
TW:Play()
wait(8)
game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
function createSound(id, volume, parent)
	assert(id, "sound id wasn't given")
	id = tostring(id):gsub("%D", "") --only keeping the numbers
	assert(id ~= "", "sound id isn't a number") 
	volume = volume or 2 --0.5 is the default volume
	parent = parent or workspace --workspace is the default parent
	local Sound = Instance.new("Sound") 
	Sound.SoundId = "rbxassetid://"..id 
	Sound.Volume = volume 
	Sound.Parent = parent 
	return Sound 
end
local Sound = createSound("rbxassetid://4398878054")
Sound.Looped = false 
Sound:Play() 
game.Lighting.MainColorCorrection.TintColor = Color3.fromRGB(255, 0, 0)
game.Lighting.MainColorCorrection.Contrast = 0.2
game.Lighting.MainColorCorrection.Saturation = -0.7
local tween = game:GetService("TweenService")
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Contrast = 0}):Play()
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Saturation = 0}):Play()
local TweenService = game:GetService("TweenService")
local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5),{TintColor = Color3.fromRGB(0, 0, 255)})
TW:Play()
wait(8)
function createSound(id, volume, parent)
	assert(id, "sound id wasn't given")
	id = tostring(id):gsub("%D", "") --only keeping the numbers
	assert(id ~= "", "sound id isn't a number") 
	volume = volume or 2 --0.5 is the default volume
	parent = parent or workspace --workspace is the default parent
	local Sound = Instance.new("Sound") 
	Sound.SoundId = "rbxassetid://"..id 
	Sound.Volume = volume 
	Sound.Parent = parent 
	return Sound 
end
local Sound = createSound("rbxassetid://4398878054")
Sound.Looped = false 
Sound:Play() 
game.Lighting.MainColorCorrection.TintColor = Color3.fromRGB(255, 0, 0)
game.Lighting.MainColorCorrection.Contrast = 0.2
game.Lighting.MainColorCorrection.Saturation = -0.7
local tween = game:GetService("TweenService")
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Contrast = 0}):Play()
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Saturation = 0}):Play()
local TweenService = game:GetService("TweenService")
local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5),{TintColor = Color3.fromRGB(0, 0, 255)})
TW:Play()
wait(8)
function createSound(id, volume, parent)
	assert(id, "sound id wasn't given")
	id = tostring(id):gsub("%D", "") --only keeping the numbers
	assert(id ~= "", "sound id isn't a number") 
	volume = volume or 2 --0.5 is the default volume
	parent = parent or workspace --workspace is the default parent
	local Sound = Instance.new("Sound") 
	Sound.SoundId = "rbxassetid://"..id 
	Sound.Volume = volume 
	Sound.Parent = parent 
	return Sound 
end
local Sound = createSound("rbxassetid://4398878054")
Sound.Looped = false 
Sound:Play() 
game.Lighting.MainColorCorrection.TintColor = Color3.fromRGB(255, 0, 0)
game.Lighting.MainColorCorrection.Contrast = 0.2
game.Lighting.MainColorCorrection.Saturation = -0.7
local tween = game:GetService("TweenService")
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Contrast = 0}):Play()
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Saturation = 0}):Play()
local TweenService = game:GetService("TweenService")
local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5),{TintColor = Color3.fromRGB(0, 0, 255)})
TW:Play()
wait(8)
function createSound(id, volume, parent)
	assert(id, "sound id wasn't given")
	id = tostring(id):gsub("%D", "") --only keeping the numbers
	assert(id ~= "", "sound id isn't a number") 
	volume = volume or 2 --0.5 is the default volume
	parent = parent or workspace --workspace is the default parent
	local Sound = Instance.new("Sound") 
	Sound.SoundId = "rbxassetid://"..id 
	Sound.Volume = volume 
	Sound.Parent = parent 
	return Sound 
end
local Sound = createSound("rbxassetid://4398878054")
Sound.Looped = false 
Sound:Play() 
game.Lighting.MainColorCorrection.TintColor = Color3.fromRGB(255, 0, 0)
game.Lighting.MainColorCorrection.Contrast = 0.2
game.Lighting.MainColorCorrection.Saturation = -0.7
local tween = game:GetService("TweenService")
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Contrast = 0}):Play()
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Saturation = 0}):Play()
local TweenService = game:GetService("TweenService")
local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5),{TintColor = Color3.fromRGB(0, 0, 255)})
TW:Play()
wait(10)
game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
function createSound(id, volume, parent)
	assert(id, "sound id wasn't given")
	id = tostring(id):gsub("%D", "") --only keeping the numbers
	assert(id ~= "", "sound id isn't a number") 
	volume = volume or 2 --0.5 is the default volume
	parent = parent or workspace --workspace is the default parent
	local Sound = Instance.new("Sound") 
	Sound.SoundId = "rbxassetid://"..id 
	Sound.Volume = volume 
	Sound.Parent = parent 
	return Sound 
end
local Sound = createSound("rbxassetid://4398878054")
Sound.Looped = false 
Sound:Play() 
game.Lighting.MainColorCorrection.TintColor = Color3.fromRGB(255, 0, 0)
game.Lighting.MainColorCorrection.Contrast = 0.2
game.Lighting.MainColorCorrection.Saturation = -0.7
local tween = game:GetService("TweenService")
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Contrast = 0}):Play()
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Saturation = 0}):Play()
local TweenService = game:GetService("TweenService")
local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5),{TintColor = Color3.fromRGB(0, 0, 255)})
TW:Play()
wait(5)
function createSound(id, volume, parent)
	assert(id, "sound id wasn't given")
	id = tostring(id):gsub("%D", "") --only keeping the numbers
	assert(id ~= "", "sound id isn't a number") 
	volume = volume or 2 --0.5 is the default volume
	parent = parent or workspace --workspace is the default parent
	local Sound = Instance.new("Sound") 
	Sound.SoundId = "rbxassetid://"..id 
	Sound.Volume = volume 
	Sound.Parent = parent 
	return Sound 
end
local Sound = createSound("rbxassetid://4398878054")
Sound.Looped = false 
Sound:Play() 
game.Lighting.MainColorCorrection.TintColor = Color3.fromRGB(255, 0, 0)
game.Lighting.MainColorCorrection.Contrast = 0.2
game.Lighting.MainColorCorrection.Saturation = -0.7
local tween = game:GetService("TweenService")
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Contrast = 0}):Play()
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Saturation = 0}):Play()
local TweenService = game:GetService("TweenService")
local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5),{TintColor = Color3.fromRGB(0, 0, 255)})
TW:Play()
wait(5)
function createSound(id, volume, parent)
	assert(id, "sound id wasn't given")
	id = tostring(id):gsub("%D", "") --only keeping the numbers
	assert(id ~= "", "sound id isn't a number") 
	volume = volume or 2 --0.5 is the default volume
	parent = parent or workspace --workspace is the default parent
	local Sound = Instance.new("Sound") 
	Sound.SoundId = "rbxassetid://"..id 
	Sound.Volume = volume 
	Sound.Parent = parent 
	return Sound 
end
local Sound = createSound("rbxassetid://4398878054")
Sound.Looped = false 
Sound:Play() 
game.Lighting.MainColorCorrection.TintColor = Color3.fromRGB(255, 0, 0)
game.Lighting.MainColorCorrection.Contrast = 0.2
game.Lighting.MainColorCorrection.Saturation = -0.7
local tween = game:GetService("TweenService")
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Contrast = 0}):Play()
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Saturation = 0}):Play()
local TweenService = game:GetService("TweenService")
local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5),{TintColor = Color3.fromRGB(0, 0, 255)})
TW:Play()
wait(9)
game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
function createSound(id, volume, parent)
	assert(id, "sound id wasn't given")
	id = tostring(id):gsub("%D", "") --only keeping the numbers
	assert(id ~= "", "sound id isn't a number") 
	volume = volume or 2 --0.5 is the default volume
	parent = parent or workspace --workspace is the default parent
	local Sound = Instance.new("Sound") 
	Sound.SoundId = "rbxassetid://"..id 
	Sound.Volume = volume 
	Sound.Parent = parent 
	return Sound 
end
local Sound = createSound("rbxassetid://4398878054")
Sound.Looped = false 
Sound:Play() 
game.Lighting.MainColorCorrection.TintColor = Color3.fromRGB(255, 0, 0)
game.Lighting.MainColorCorrection.Contrast = 0.2
game.Lighting.MainColorCorrection.Saturation = -0.7
local tween = game:GetService("TweenService")
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Contrast = 0}):Play()
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Saturation = 0}):Play()
local TweenService = game:GetService("TweenService")
local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5),{TintColor = Color3.fromRGB(0, 0, 255)})
TW:Play()
wait(7)
function createSound(id, volume, parent)
	assert(id, "sound id wasn't given")
	id = tostring(id):gsub("%D", "") --only keeping the numbers
	assert(id ~= "", "sound id isn't a number") 
	volume = volume or 2 --0.5 is the default volume
	parent = parent or workspace --workspace is the default parent
	local Sound = Instance.new("Sound") 
	Sound.SoundId = "rbxassetid://"..id 
	Sound.Volume = volume 
	Sound.Parent = parent 
	return Sound 
end
local Sound = createSound("rbxassetid://4398878054")
Sound.Looped = false 
Sound:Play() 
game.Lighting.MainColorCorrection.TintColor = Color3.fromRGB(255, 0, 0)
game.Lighting.MainColorCorrection.Contrast = 0.2
game.Lighting.MainColorCorrection.Saturation = -0.7
local tween = game:GetService("TweenService")
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Contrast = 0}):Play()
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Saturation = 0}):Play()
TW:Play()
wait(7)
game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
function createSound(id, volume, parent)
	assert(id, "sound id wasn't given")
	id = tostring(id):gsub("%D", "") --only keeping the numbers
	assert(id ~= "", "sound id isn't a number") 
	volume = volume or 2 --0.5 is the default volume
	parent = parent or workspace --workspace is the default parent
	local Sound = Instance.new("Sound") 
	Sound.SoundId = "rbxassetid://"..id 
	Sound.Volume = volume 
	Sound.Parent = parent 
	return Sound 
end
local Sound = createSound("rbxassetid://4398878054")
Sound.Looped = false 
Sound:Play() 
game.Lighting.MainColorCorrection.TintColor = Color3.fromRGB(255, 0, 0)
game.Lighting.MainColorCorrection.Contrast = 0.2
game.Lighting.MainColorCorrection.Saturation = -0.7
local tween = game:GetService("TweenService")
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Contrast = 0}):Play()
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Saturation = 0}):Play()
TW:Play()
wait(7)
game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
function createSound(id, volume, parent)
	assert(id, "sound id wasn't given")
	id = tostring(id):gsub("%D", "") --only keeping the numbers
	assert(id ~= "", "sound id isn't a number") 
	volume = volume or 2 --0.5 is the default volume
	parent = parent or workspace --workspace is the default parent
	local Sound = Instance.new("Sound") 
	Sound.SoundId = "rbxassetid://"..id 
	Sound.Volume = volume 
	Sound.Parent = parent 
	return Sound 
end
local Sound = createSound("rbxassetid://4398878054")
Sound.Looped = false 
Sound:Play() 
game.Lighting.MainColorCorrection.TintColor = Color3.fromRGB(255, 0, 0)
game.Lighting.MainColorCorrection.Contrast = 0.2
game.Lighting.MainColorCorrection.Saturation = -0.7
local tween = game:GetService("TweenService")
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Contrast = 0}):Play()
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Saturation = 0}):Play()
local Creator = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors%20Entity%20Spawner/Source.lua"))()
local entity = Creator.createEntity({
    CustomName = "Creepy Teeth", -- Custom name of your entity
    Model = "rbxassetid://12805768832", -- Can be GitHub file or rbxassetid
    Speed = 375, -- Percentage, 100 = default Rush speed
    DelayTime = 1.5, -- Time before starting cycles (seconds)
    HeightOffset = 0,
    CanKill = true,
    BreakLights = false,
    KillRange = 40,
    BackwardsMovement = false,
    FlickerLights = {
        false, -- Enabled
        2.5, -- Time (seconds)
    },
    Cycles = {
        Min = 10,
        Max = 10
    },
    CamShake = {
        true, -- Enabled
        {5, 15, 0.1, 1}, -- Shake values (don't change if you don't know)
        100, -- Shake start distance (from Entity to you)
    },
    Jumpscare = {
        true, -- Enabled ('false' if you don't want jumpscare)
        {
            Image1 = "", -- Image1 url
            Image2 = "", -- Image2 url
            Shake = false,
            Sound1 = {
                0, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Sound2 = {
                5351101493, -- SoundId
                { Volume = 5 }, -- Sound properties
            },
            Flashing = {
                true, -- Enabled
                Color3.fromRGB(6, 38, 135), -- Color
            },
            Tease = {
                false, -- Enabled ('false' if you don't want tease)
                Min = 1,
                Max = 3,
            },
        },
    },
    CustomDialog = {"Bạn Đã Chết Bởi Thực Thể Creepy Teeth", "Nó có ngoại hình nhìn rất đáng sợ và di chuyển rất nhanh", "Hãy Cảm Nhận Các Dấu Hiệu Bất Thường Trong Môi Trường Xung Quanh", "Bởi vì nó có thể XUẤT HIỆN khi nào bạn không hề biết"}, -- Custom death message (can be as long as you want)
})

-----[[ Advanced ]]-----

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity:", entity, "has entered room:", room)
    Speed = 0
    wait(2)
    Speed = 170
end

entity.Debug.OnEntitySpawned = function(entityModel)
    print("Entity has spawned:", entityModel)
local bruh = workspace.Rebound.Torso
local TweenService = game:GetService("TweenService")
local spawn = Instance.new("Sound")
spawn.Parent = bruh
spawn.Name = "ReboundSpawn"
spawn.SoundId = "rbxassetid://9114221327"
spawn.Volume = 5
spawn.RollOffMaxDistance = 10000
spawn.RollOffMinDistance = 450
spawn:Play()
    local move = GetGitSound("https://github.com/check78/worldcuuuup/blob/main/DoomBegin.mp3?raw=true","Reboun")
    move.Parent = bruh
    move.Name = "ReboundMoving"
    move.Volume = 0
    move.Looped = true
local vroom = TweenService:Create(move, TweenInfo.new(2),{Volume = 0.2})
    local distort = Instance.new("DistortionSoundEffect")
    distort.Level = 0.75
    distort.Parent = move
	move.RollOffMaxDistance = 200
	move.RollOffMinDistance = 100
    local tree = Instance.new("TremoloSoundEffect")
    tree.Depth = 1
    tree.Duty = 1
    tree.Frequency = 5
    tree.Parent = move
    local eq = Instance.new("EqualizerSoundEffect")
    eq.HighGain = -60
    eq.MidGain = 10
    eq.LowGain = 10
    eq.Parent = move
	wait(1)
    vroom:Play()
	move:Play()
end

entity.Debug.OnEntityDespawned = function(entityModel)
    print("Entity has despawned:", entityModel)
end

entity.Debug.OnEntityStartMoving = function(entityModel)
    print("Entity has started moving:", entityModel)
end

entity.Debug.OnEntityFinishedRebound = function(entityModel)
    print("Entity finished rebound:", entityModel)
end

entity.Debug.OnDeath = function()
    warn("You died.")
local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(15),{TintColor = Color3.fromRGB(255, 0, 0)})
TW:Play()
    firesignal(game.ReplicatedStorage.EntityInfo.DeathHint.OnClientEvent, {"Bạn Đã Chết Bởi Thực Thể Creepy Teeth", "Nó có ngoại hình nhìn rất đáng sợ và di chuyển rất nhanh", "Hãy Cảm Nhận Các Dấu Hiệu Bất Thường Trong Môi Trường Xung Quanh", "Bởi vì nó có thể XUẤT HIỆN khi nào bạn không hề biết"}, "Blue")

	local ReSt = game.ReplicatedStorage
	local Plr = game.Players.LocalPlayer

		scare:Play()
	    ReSt.GameStats["Player_".. Plr.Name].Total.DeathCause.Value = "Rebound"
		wait(0.5)
		scare:Destroy()

end

------------------------

-- Run the created entity
Creator.runEntity(entity)
-----

game.Lighting.MainColorCorrection.TintColor = Color3.fromRGB(255, 0, 0)
game.Lighting.MainColorCorrection.Contrast = 0.2
game.Lighting.MainColorCorrection.Saturation = -0.7
local tween = game:GetService("TweenService")
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Contrast = 0}):Play()
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Saturation = 0}):Play()
local TweenService = game:GetService("TweenService")
local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5),{TintColor = Color3.fromRGB(0, 0, 255)})
TW:Play()
-- Achievement
local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()
Achievements.Get({
    Title = "Thật Là Đỏ Thẫm",
    Desc = "Sóng Sót Khỏi Thực Thể Siêu Hiếm Creepy Teeth",
    Reason = "Tithy Creepy Teeth",
    Image = "https://i.ibb.co/fMSy6DW/image-1.png",
})
-- ALL
local Creator = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors%20Entity%20Spawner/Source.lua"))()

-- Create entity

local entity = Creator.createEntity({
    CustomName = "A-50", -- Custom name of your entity
    Model = "rbxassetid://11470738519", -- Can be GitHub file or rbxassetid
    Speed = 100, -- Percentage, 100 = default Rush speed
    DelayTime = 3, -- Time before starting cycles (seconds)
    HeightOffset = 0,
    CanKill = true,
    BreakLights = false,
    KillRange = 40,
    BackwardsMovement = false,
    FlickerLights = {
        false, -- Enabled
        2.5, -- Time (seconds)
    },
    Cycles = {
        Min = 1,
        Max = 1
    },
    CamShake = {
        true, -- Enabled
        {5, 15, 0.1, 1}, -- Shake values (don't change if you don't know)
        100, -- Shake start distance (from Entity to you)
    },
    Jumpscare = {
        false, -- Enabled ('false' if you don't want jumpscare)
        {
            Image1 = "https://tr.rbxcdn.com/1e7945796277100aec5f98858407b523/420/420/Decal/Png", -- Image1 url
            Image2 = "https://tr.rbxcdn.com/1e7945796277100aec5f98858407b523/420/420/Decal/Png", -- Image2 url
            Shake = false,
            Sound1 = {
                5351101493, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Sound2 = {
                5351101493, -- SoundId
                { Volume = 5 }, -- Sound properties
            },
            Flashing = {
                true, -- Enabled
                Color3.fromRGB(6, 38, 135), -- Color
            },
            Tease = {
                false, -- Enabled ('false' if you don't want tease)
                Min = 1,
                Max = 3,
            },
        },
    },
    CustomDialog = {"..."}, -- Custom death message (can be as long as you want)
})

-----[[ Advanced ]]-----

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity:", entity, "has entered room:", room)
    Speed = 0
    wait(2)
    Speed = 170
end

entity.Debug.OnEntitySpawned = function(entityModel)
    print("Entity has spawned:", entityModel)
local bruh = workspace.Rebound.Torso
local TweenService = game:GetService("TweenService")
local spawn = Instance.new("Sound")
spawn.Parent = bruh
spawn.Name = "ReboundSpawn"
spawn.SoundId = "rbxassetid://9114221327"
spawn.Volume = 5
spawn.RollOffMaxDistance = 10000
spawn.RollOffMinDistance = 450
spawn:Play()
    local move = GetGitSound("https://github.com/check78/worldcuuuup/blob/main/DoomBegin.mp3?raw=true","Reboun")
    move.Parent = bruh
    move.Name = "ReboundMoving"
    move.Volume = 0
    move.Looped = true
local vroom = TweenService:Create(move, TweenInfo.new(2),{Volume = 0.2})
    local distort = Instance.new("DistortionSoundEffect")
    distort.Level = 0.75
    distort.Parent = move
	move.RollOffMaxDistance = 200
	move.RollOffMinDistance = 100
    local tree = Instance.new("TremoloSoundEffect")
    tree.Depth = 1
    tree.Duty = 1
    tree.Frequency = 5
    tree.Parent = move
    local eq = Instance.new("EqualizerSoundEffect")
    eq.HighGain = -60
    eq.MidGain = 10
    eq.LowGain = 10
    eq.Parent = move
	wait(1)
    vroom:Play()
	move:Play()
end

entity.Debug.OnEntityDespawned = function(entityModel)
    print("Entity has despawned:", entityModel)
end

entity.Debug.OnEntityStartMoving = function(entityModel)
    print("Entity has started moving:", entityModel)
end

entity.Debug.OnEntityFinishedRebound = function(entityModel)
    print("Entity finished rebound:", entityModel)
	
end

entity.Debug.OnDeath = function()
    warn("You died.")
local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(15),{TintColor = Color3.fromRGB(255, 0, 0)})
TW:Play()
    firesignal(game.ReplicatedStorage.EntityInfo.DeathHint.OnClientEvent, {"..."}, "Blue")

	local ReSt = game.ReplicatedStorage
	local Plr = game.Players.LocalPlayer

		scare:Play()
	    ReSt.GameStats["Player_".. Plr.Name].Total.DeathCause.Value = "Rebound"
		wait(0.5)
		scare:Destroy()
end

------------------------

-- Run the created entity
Creator.runEntity(entity)
function createSound(id, volume, parent)
	assert(id, "sound id wasn't given")
	id = tostring(id):gsub("%D", "") --only keeping the numbers
	assert(id ~= "", "sound id isn't a number") 
	volume = volume or 0.5 --0.5 is the default volume
	parent = parent or workspace --workspace is the default parent
	local Sound = Instance.new("Sound") 
	Sound.SoundId = "rbxassetid://"..id 
	Sound.Volume = volume 
	Sound.Parent = parent 
	return Sound 
end
local Sound = createSound("rbxassetid://9120829080")
Sound.Looped = false 
Sound:Play() 
local CameraShaker = require(game.ReplicatedStorage.CameraShaker)
local camara = game.Workspace.CurrentCamera
local camShake = CameraShaker.new(Enum.RenderPriority.Camera.Value, function(shakeCf)
	camara.CFrame = camara.CFrame * shakeCf
end)
camShake:Start()
camShake:ShakeOnce(15,8.8,0,2,1,6)
-- SAVAGE SMILE REDOUBLE
wait(10)
game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
wait(0.5)
game.Lighting.MainColorCorrection.TintColor = Color3.fromRGB(255, 0, 0)
game.Lighting.MainColorCorrection.Contrast = 0.2
game.Lighting.MainColorCorrection.Saturation = -0.7
local tween = game:GetService("TweenService")
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Contrast = 0}):Play()
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Saturation = 0}):Play()
local TweenService = game:GetService("TweenService")
local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5),{TintColor = Color3.fromRGB(0, 0, 255)})
TW:Play()

function createSound(id, volume, parent)
	assert(id, "sound id wasn't given")
	id = tostring(id):gsub("%D", "") --only keeping the numbers
	assert(id ~= "", "sound id isn't a number") 
	volume = volume or 2 --0.5 is the default volume
	parent = parent or workspace --workspace is the default parent
	local Sound = Instance.new("Sound") 
	Sound.SoundId = "rbxassetid://"..id 
	Sound.Volume = volume 
	Sound.Parent = parent 
	return Sound 
end
local Sound = createSound("rbxassetid://5268594777")
Sound.Looped = false 
Sound:Play()
wait(5)  
local Creator = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors%20Entity%20Spawner/Source.lua"))()

-- Create entity

local entity = Creator.createEntity({
    CustomName = "Savage Smile", -- Custom name of your entity
    Model = "rbxassetid://12645075460", -- Can be GitHub file or rbxassetid
    Speed = 100, -- Percentage, 100 = default Rush speed
    DelayTime = 3, -- Time before starting cycles (seconds)
    HeightOffset = 0,
    CanKill = true,
    BreakLights = false,
    KillRange = 40,
    BackwardsMovement = true,
    FlickerLights = {
        true, -- Enabled
        2.5, -- Time (seconds)
    },
    Cycles = {
        Min = 3,
        Max = 3
    },
    CamShake = {
        true, -- Enabled
        {5, 15, 0.1, 1}, -- Shake values (don't change if you don't know)
        100, -- Shake start distance (from Entity to you)
    },
    Jumpscare = {
        true, -- Enabled ('false' if you don't want jumpscare)
        {
            Image1 = "https://tr.rbxcdn.com/1e7945796277100aec5f98858407b523/420/420/Decal/Png", -- Image1 url
            Image2 = "https://tr.rbxcdn.com/1e7945796277100aec5f98858407b523/420/420/Decal/Png", -- Image2 url
            Shake = false,
            Sound1 = {
                0, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Sound2 = {
                5351101493, -- SoundId
                { Volume = 5 }, -- Sound properties
            },
            Flashing = {
                true, -- Enabled
                Color3.fromRGB(6, 38, 135), -- Color
            },
            Tease = {
                false, -- Enabled ('false' if you don't want tease)
                Min = 1,
                Max = 3,
            },
        },
    },
    CustomDialog = {"Bạn Đã Chết Bởi Thực Thể Nào Đó Gọi là Savage Smile", "Hãy Chủng Bị Tinh Thần Trước Khi Nghe Tiếng La Hét Của Nó", "E̸̩̠̠͙̜͓͔͔̮̾R̴̨͈̙̣̙̟̼̹͇͊̊̔̈́͂͗͒̊̋́̔͗̑̀͝R̵̡̧͍̱̥̤̞͓̗̰̩͗̓͒̿̊͋̒͝Ǫ̵̦̳̰̻̹̳̖̦͇̝̅̅̀͂͊̊̈́̏̈̽̓͝R̵̛͕̙͉̥̤͕̹̪̄́̈́̽́̓̈́́̈́̍͌", "..."}, -- Custom death message (can be as long as you want)
})

-----[[ Advanced ]]-----

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity:", entity, "has entered room:", room)
    Speed = 0
    wait(2)
    Speed = 170
end

entity.Debug.OnEntitySpawned = function(entityModel)
    print("Entity has spawned:", entityModel)
local bruh = workspace.Rebound.Torso
local TweenService = game:GetService("TweenService")
local spawn = Instance.new("Sound")
spawn.Parent = bruh
spawn.Name = "ReboundSpawn"
spawn.SoundId = "rbxassetid://9114221327"
spawn.Volume = 5
spawn.RollOffMaxDistance = 10000
spawn.RollOffMinDistance = 450
spawn:Play()
    local move = GetGitSound("https://github.com/check78/worldcuuuup/blob/main/DoomBegin.mp3?raw=true","Reboun")
    move.Parent = bruh
    move.Name = "ReboundMoving"
    move.Volume = 0
    move.Looped = true
local vroom = TweenService:Create(move, TweenInfo.new(2),{Volume = 0.2})
    local distort = Instance.new("DistortionSoundEffect")
    distort.Level = 0.75
    distort.Parent = move
	move.RollOffMaxDistance = 200
	move.RollOffMinDistance = 100
    local tree = Instance.new("TremoloSoundEffect")
    tree.Depth = 1
    tree.Duty = 1
    tree.Frequency = 5
    tree.Parent = move
    local eq = Instance.new("EqualizerSoundEffect")
    eq.HighGain = -60
    eq.MidGain = 10
    eq.LowGain = 10
    eq.Parent = move
	wait(1)
    vroom:Play()
	move:Play()
end

entity.Debug.OnEntityDespawned = function(entityModel)
    print("Entity has despawned:", entityModel)
end

entity.Debug.OnEntityStartMoving = function(entityModel)
    print("Entity has started moving:", entityModel)
end

entity.Debug.OnEntityFinishedRebound = function(entityModel)
    print("Entity finished rebound:", entityModel)
	
end

entity.Debug.OnDeath = function()
    warn("You died.")
local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(15),{TintColor = Color3.fromRGB(255, 0, 0)})
TW:Play()
    firesignal(game.ReplicatedStorage.EntityInfo.DeathHint.OnClientEvent, {"Bạn Đã Chết Bởi Thực Thể Nào Đó Gọi là Savage Smile", "Hãy Chủng Bị Tinh Thần Trước Khi Nghe Tiếng La Hét Của Nó", "E̸̩̠̠͙̜͓͔͔̮̾R̴̨͈̙̣̙̟̼̹͇͊̊̔̈́͂͗͒̊̋́̔͗̑̀͝R̵̡̧͍̱̥̤̞͓̗̰̩͗̓͒̿̊͋̒͝Ǫ̵̦̳̰̻̹̳̖̦͇̝̅̅̀͂͊̊̈́̏̈̽̓͝R̵̛͕̙͉̥̤͕̹̪̄́̈́̽́̓̈́́̈́̍͌", "..."}, "Blue")

	local ReSt = game.ReplicatedStorage
	local Plr = game.Players.LocalPlayer

		scare:Play()
	    ReSt.GameStats["Player_".. Plr.Name].Total.DeathCause.Value = "Rebound"
		wait(0.5)
		scare:Destroy()

end
--
local Creator = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors%20Entity%20Spawner/Source.lua"))()

-- Create entity

local entity = Creator.createEntity({
    CustomName = "A-50", -- Custom name of your entity
    Model = "rbxassetid://11470738519", -- Can be GitHub file or rbxassetid
    Speed = 100, -- Percentage, 100 = default Rush speed
    DelayTime = 3, -- Time before starting cycles (seconds)
    HeightOffset = 0,
    CanKill = true,
    BreakLights = false,
    KillRange = 40,
    BackwardsMovement = false,
    FlickerLights = {
        false, -- Enabled
        2.5, -- Time (seconds)
    },
    Cycles = {
        Min = 1,
        Max = 1
    },
    CamShake = {
        true, -- Enabled
        {5, 15, 0.1, 1}, -- Shake values (don't change if you don't know)
        100, -- Shake start distance (from Entity to you)
    },
    Jumpscare = {
        false, -- Enabled ('false' if you don't want jumpscare)
        {
            Image1 = "https://tr.rbxcdn.com/1e7945796277100aec5f98858407b523/420/420/Decal/Png", -- Image1 url
            Image2 = "https://tr.rbxcdn.com/1e7945796277100aec5f98858407b523/420/420/Decal/Png", -- Image2 url
            Shake = false,
            Sound1 = {
                5351101493, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Sound2 = {
                5351101493, -- SoundId
                { Volume = 5 }, -- Sound properties
            },
            Flashing = {
                true, -- Enabled
                Color3.fromRGB(6, 38, 135), -- Color
            },
            Tease = {
                false, -- Enabled ('false' if you don't want tease)
                Min = 1,
                Max = 3,
            },
        },
    },
    CustomDialog = {"..."}, -- Custom death message (can be as long as you want)
})

-----[[ Advanced ]]-----

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity:", entity, "has entered room:", room)
    Speed = 0
    wait(2)
    Speed = 170
end

entity.Debug.OnEntitySpawned = function(entityModel)
    print("Entity has spawned:", entityModel)
local bruh = workspace.Rebound.Torso
local TweenService = game:GetService("TweenService")
local spawn = Instance.new("Sound")
spawn.Parent = bruh
spawn.Name = "ReboundSpawn"
spawn.SoundId = "rbxassetid://9114221327"
spawn.Volume = 5
spawn.RollOffMaxDistance = 10000
spawn.RollOffMinDistance = 450
spawn:Play()
    local move = GetGitSound("https://github.com/check78/worldcuuuup/blob/main/DoomBegin.mp3?raw=true","Reboun")
    move.Parent = bruh
    move.Name = "ReboundMoving"
    move.Volume = 0
    move.Looped = true
local vroom = TweenService:Create(move, TweenInfo.new(2),{Volume = 0.2})
    local distort = Instance.new("DistortionSoundEffect")
    distort.Level = 0.75
    distort.Parent = move
	move.RollOffMaxDistance = 200
	move.RollOffMinDistance = 100
    local tree = Instance.new("TremoloSoundEffect")
    tree.Depth = 1
    tree.Duty = 1
    tree.Frequency = 5
    tree.Parent = move
    local eq = Instance.new("EqualizerSoundEffect")
    eq.HighGain = -60
    eq.MidGain = 10
    eq.LowGain = 10
    eq.Parent = move
	wait(1)
    vroom:Play()
	move:Play()
end

entity.Debug.OnEntityDespawned = function(entityModel)
    print("Entity has despawned:", entityModel)
end

entity.Debug.OnEntityStartMoving = function(entityModel)
    print("Entity has started moving:", entityModel)
end

entity.Debug.OnEntityFinishedRebound = function(entityModel)
    print("Entity finished rebound:", entityModel)
	
end

entity.Debug.OnDeath = function()
    warn("You died.")
local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(15),{TintColor = Color3.fromRGB(255, 0, 0)})
TW:Play()
    firesignal(game.ReplicatedStorage.EntityInfo.DeathHint.OnClientEvent, {"..."}, "Blue")

	local ReSt = game.ReplicatedStorage
	local Plr = game.Players.LocalPlayer

		scare:Play()
	    ReSt.GameStats["Player_".. Plr.Name].Total.DeathCause.Value = "Rebound"
		wait(0.5)
		scare:Destroy()
end

------------------------

-- Run the created entity
Creator.runEntity(entity)
function createSound(id, volume, parent)
	assert(id, "sound id wasn't given")
	id = tostring(id):gsub("%D", "") --only keeping the numbers
	assert(id ~= "", "sound id isn't a number") 
	volume = volume or 0.5 --0.5 is the default volume
	parent = parent or workspace --workspace is the default parent
	local Sound = Instance.new("Sound") 
	Sound.SoundId = "rbxassetid://"..id 
	Sound.Volume = volume 
	Sound.Parent = parent 
	return Sound 
end
local Sound = createSound("rbxassetid://9120829080")
Sound.Looped = false 
Sound:Play() 
local CameraShaker = require(game.ReplicatedStorage.CameraShaker)
local camara = game.Workspace.CurrentCamera
local camShake = CameraShaker.new(Enum.RenderPriority.Camera.Value, function(shakeCf)
	camara.CFrame = camara.CFrame * shakeCf
end)
camShake:Start()
camShake:ShakeOnce(15,8.8,0,2,1,6)
------------------------

-- Run the created entity
Creator.runEntity(entity)
--------
function createSound(id, volume, parent)
	assert(id, "sound id wasn't given")
	id = tostring(id):gsub("%D", "") --only keeping the numbers
	assert(id ~= "", "sound id isn't a number") 
	volume = volume or 10 --0.5 is the default volume
	parent = parent or workspace --workspace is the default parent
	local Sound = Instance.new("Sound") 
	Sound.SoundId = "rbxassetid://"..id 
	Sound.Volume = volume 
	Sound.Parent = parent 
	return Sound 
end
local Sound = createSound("rbxassetid://5408539313")
Sound.Looped = false 
Sound:Play() 
game.Lighting.MainColorCorrection.TintColor = Color3.fromRGB(0, 0, 0)
game.Lighting.MainColorCorrection.Contrast = 0.2
game.Lighting.MainColorCorrection.Saturation = -0.7
local tween = game:GetService("TweenService")
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Contrast = 0}):Play()
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Saturation = 0}):Play()
local TweenService = game:GetService("TweenService")
local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5),{TintColor = Color3.fromRGB(0, 0, 255)})
TW:Play()
------------------------
-- Blood Eyes
wait(15)
game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
loadstring(game:HttpGet("https://raw.githubusercontent.com/idontknowwhatmyname/blank/main/BloodEyes"))()
--------------------
-- Depth Entity
task.wait(95)
game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
function createSound(id, volume, parent)
	assert(id, "sound id wasn't given")
	id = tostring(id):gsub("%D", "") --only keeping the numbers
	assert(id ~= "", "sound id isn't a number") 
	volume = volume or 2 --0.5 is the default volume
	parent = parent or workspace --workspace is the default parent
	local Sound = Instance.new("Sound") 
	Sound.SoundId = "rbxassetid://"..id 
	Sound.Volume = volume 
	Sound.Parent = parent 
	return Sound 
end
local Sound = createSound("rbxassetid://2738830850")
Sound.Looped = false 
Sound:Play()
wait(17)  
local Creator = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors%20Entity%20Spawner/Source.lua"))()

-- Create entity

local entity = Creator.createEntity({
    CustomName = "Depth", -- Custom name of your entity
    Model = "rbxassetid://12134112291", -- Can be GitHub file or rbxassetid
    Speed = 500, -- Percentage, 100 = default Rush speed
    DelayTime = 5, -- Time before starting cycles (seconds)
    HeightOffset = 0,
    CanKill = true,
    BreakLights = false,
    KillRange = 500,
    BackwardsMovement = false,
    FlickerLights = {
        true, -- Enabled
        3.5, -- Time (seconds)
    },
    Cycles = {
        Min = 8,
        Max = 8
    },
    CamShake = {
        true, -- Enabled
        {5, 15, 0.1, 1}, -- Shake values (don't change if you don't know)
        100, -- Shake start distance (from Entity to you)
    },
    Jumpscare = {
        true, -- Enabled ('false' if you don't want jumpscare)
        {
            Image1 = "https://tr.rbxcdn.com/2f9b6d5df515f652e2d4eb816a1547c6/420/420/Decal/Png", -- Image1 url
            Image2 = "https://tr.rbxcdn.com/2f9b6d5df515f652e2d4eb816a1547c6/420/420/Decal/Png", -- Image2 url
            Shake = false,
            Sound1 = {
                0, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Sound2 = {
                5351101493, -- SoundId
                { Volume = 5 }, -- Sound properties
            },
            Flashing = {
                true, -- Enabled
                Color3.fromRGB(6, 38, 135), -- Color
            },
            Tease = {
                false, -- Enabled ('false' if you don't want tease)
                Min = 1,
                Max = 3,
            },
        },
    },
    CustomDialog = {"Bạn Đã Chết Bởi Thực Thể Nào Đó Gọi là Depth", "Chúng Di Chuyển Rất Nhanh Hãy Cẩn Thận", "Hãy Thực Hiện Việc Ra Vào Tủ Nhiều Lần Nhưng Phải Thật Nhanh", "..."}, -- Custom death message (can be as long as you want)
})

-----[[ Advanced ]]-----

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity:", entity, "has entered room:", room)
    Speed = 0
    wait(2)
    Speed = 170
end

entity.Debug.OnEntitySpawned = function(entityModel)
    print("Entity has spawned:", entityModel)
local bruh = workspace.Rebound.Torso
local TweenService = game:GetService("TweenService")
local spawn = Instance.new("Sound")
spawn.Parent = bruh
spawn.Name = "ReboundSpawn"
spawn.SoundId = "rbxassetid://9114221327"
spawn.Volume = 5
spawn.RollOffMaxDistance = 10000
spawn.RollOffMinDistance = 450
spawn:Play()
    local move = GetGitSound("https://github.com/check78/worldcuuuup/blob/main/DoomBegin.mp3?raw=true","Reboun")
    move.Parent = bruh
    move.Name = "ReboundMoving"
    move.Volume = 0
    move.Looped = true
local vroom = TweenService:Create(move, TweenInfo.new(2),{Volume = 0.2})
    local distort = Instance.new("DistortionSoundEffect")
    distort.Level = 0.75
    distort.Parent = move
	move.RollOffMaxDistance = 200
	move.RollOffMinDistance = 100
    local tree = Instance.new("TremoloSoundEffect")
    tree.Depth = 1
    tree.Duty = 1
    tree.Frequency = 5
    tree.Parent = move
    local eq = Instance.new("EqualizerSoundEffect")
    eq.HighGain = -60
    eq.MidGain = 10
    eq.LowGain = 10
    eq.Parent = move
	wait(1)
    vroom:Play()
	move:Play()
end

entity.Debug.OnEntityDespawned = function(entityModel)
    print("Entity has despawned:", entityModel)
end

entity.Debug.OnEntityStartMoving = function(entityModel)
    print("Entity has started moving:", entityModel)
end

entity.Debug.OnEntityFinishedRebound = function(entityModel)
    print("Entity finished rebound:", entityModel)
	
end

entity.Debug.OnDeath = function()
    warn("You died.")
local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(15),{TintColor = Color3.fromRGB(255, 0, 0)})
TW:Play()
    firesignal(game.ReplicatedStorage.EntityInfo.DeathHint.OnClientEvent, {"Bạn Đã Chết Bởi Thực Thể Nào Đó Gọi là Depth", "Chúng Di Chuyển Rất Nhanh Hãy Cẩn Thận", "Hãy Thực Hiện Việc Ra Vào Tủ Nhiều Lần Nhưng Phải Thật Nhanh", "..."}, "Blue")

	local ReSt = game.ReplicatedStorage
	local Plr = game.Players.LocalPlayer

		scare:Play()
	    ReSt.GameStats["Player_".. Plr.Name].Total.DeathCause.Value = "Rebound"
		wait(0.5)
		scare:Destroy()
end

------------------------

-- Run the created entity
Creator.runEntity(entity)
wait(95)
-- Doors 1 checkpoint wait
game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
-- Doors 2 checkpoint wait
game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
-- Doors 3 checkpoint wait
game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
-- Doors 4 checkpoint wait
local Creator = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors%20Entity%20Spawner/Source.lua"))()

-- Create entity

local entity = Creator.createEntity({
    CustomName = "A-50", -- Custom name of your entity
    Model = "rbxassetid://11470738519", -- Can be GitHub file or rbxassetid
    Speed = 100, -- Percentage, 100 = default Rush speed
    DelayTime = 3, -- Time before starting cycles (seconds)
    HeightOffset = 0,
    CanKill = true,
    BreakLights = false,
    KillRange = 40,
    BackwardsMovement = false,
    FlickerLights = {
        false, -- Enabled
        2.5, -- Time (seconds)
    },
    Cycles = {
        Min = 1,
        Max = 1
    },
    CamShake = {
        true, -- Enabled
        {5, 15, 0.1, 1}, -- Shake values (don't change if you don't know)
        100, -- Shake start distance (from Entity to you)
    },
    Jumpscare = {
        false, -- Enabled ('false' if you don't want jumpscare)
        {
            Image1 = "https://tr.rbxcdn.com/1e7945796277100aec5f98858407b523/420/420/Decal/Png", -- Image1 url
            Image2 = "https://tr.rbxcdn.com/1e7945796277100aec5f98858407b523/420/420/Decal/Png", -- Image2 url
            Shake = false,
            Sound1 = {
                5351101493, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Sound2 = {
                5351101493, -- SoundId
                { Volume = 5 }, -- Sound properties
            },
            Flashing = {
                true, -- Enabled
                Color3.fromRGB(6, 38, 135), -- Color
            },
            Tease = {
                false, -- Enabled ('false' if you don't want tease)
                Min = 1,
                Max = 3,
            },
        },
    },
    CustomDialog = {"..."}, -- Custom death message (can be as long as you want)
})

-----[[ Advanced ]]-----

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity:", entity, "has entered room:", room)
    Speed = 0
    wait(2)
    Speed = 170
end

entity.Debug.OnEntitySpawned = function(entityModel)
    print("Entity has spawned:", entityModel)
local bruh = workspace.Rebound.Torso
local TweenService = game:GetService("TweenService")
local spawn = Instance.new("Sound")
spawn.Parent = bruh
spawn.Name = "ReboundSpawn"
spawn.SoundId = "rbxassetid://9114221327"
spawn.Volume = 5
spawn.RollOffMaxDistance = 10000
spawn.RollOffMinDistance = 450
spawn:Play()
    local move = GetGitSound("https://github.com/check78/worldcuuuup/blob/main/DoomBegin.mp3?raw=true","Reboun")
    move.Parent = bruh
    move.Name = "ReboundMoving"
    move.Volume = 0
    move.Looped = true
local vroom = TweenService:Create(move, TweenInfo.new(2),{Volume = 0.2})
    local distort = Instance.new("DistortionSoundEffect")
    distort.Level = 0.75
    distort.Parent = move
	move.RollOffMaxDistance = 200
	move.RollOffMinDistance = 100
    local tree = Instance.new("TremoloSoundEffect")
    tree.Depth = 1
    tree.Duty = 1
    tree.Frequency = 5
    tree.Parent = move
    local eq = Instance.new("EqualizerSoundEffect")
    eq.HighGain = -60
    eq.MidGain = 10
    eq.LowGain = 10
    eq.Parent = move
	wait(1)
    vroom:Play()
	move:Play()
end

entity.Debug.OnEntityDespawned = function(entityModel)
    print("Entity has despawned:", entityModel)
end

entity.Debug.OnEntityStartMoving = function(entityModel)
    print("Entity has started moving:", entityModel)
end

entity.Debug.OnEntityFinishedRebound = function(entityModel)
    print("Entity finished rebound:", entityModel)
	
end

entity.Debug.OnDeath = function()
    warn("You died.")
local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(15),{TintColor = Color3.fromRGB(255, 0, 0)})
TW:Play()
    firesignal(game.ReplicatedStorage.EntityInfo.DeathHint.OnClientEvent, {"..."}, "Blue")

	local ReSt = game.ReplicatedStorage
	local Plr = game.Players.LocalPlayer

		scare:Play()
	    ReSt.GameStats["Player_".. Plr.Name].Total.DeathCause.Value = "Rebound"
		wait(0.5)
		scare:Destroy()
end

------------------------

-- Run the created entity
Creator.runEntity(entity)
function createSound(id, volume, parent)
	assert(id, "sound id wasn't given")
	id = tostring(id):gsub("%D", "") --only keeping the numbers
	assert(id ~= "", "sound id isn't a number") 
	volume = volume or 0.5 --0.5 is the default volume
	parent = parent or workspace --workspace is the default parent
	local Sound = Instance.new("Sound") 
	Sound.SoundId = "rbxassetid://"..id 
	Sound.Volume = volume 
	Sound.Parent = parent 
	return Sound 
end
local Sound = createSound("rbxassetid://9120829080")
Sound.Looped = false 
Sound:Play() 
local CameraShaker = require(game.ReplicatedStorage.CameraShaker)
local camara = game.Workspace.CurrentCamera
local camShake = CameraShaker.new(Enum.RenderPriority.Camera.Value, function(shakeCf)
	camara.CFrame = camara.CFrame * shakeCf
end)
camShake:Start()
camShake:ShakeOnce(15,8.8,0,2,1,6)
game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
-- Doors 5 checkpoint wait
game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
-- Doors 6 checkpoint wait
game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
-- Blood Ambush Spawn
workspace["Ambience_Ambush"]:Play()
local CameraShaker = require(game.ReplicatedStorage.CameraShaker)
local camara = game.Workspace.CurrentCamera
local camShake = CameraShaker.new(Enum.RenderPriority.Camera.Value, function(shakeCf)
	camara.CFrame = camara.CFrame * shakeCf
end)
camShake:Start()
camShake:ShakeOnce(10,3,0.1,6,2,0.5)
wait(6)
local Creator = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors%20Entity%20Spawner/Source.lua"))()
local entity = Creator.createEntity({
    CustomName = "Blood Ambush", -- Custom name of your entity
    Model = "rbxassetid://12304880388", -- Can be GitHub file or rbxassetid
    Speed = 260, -- Percentage, 100 = default Rush speed
    DelayTime = 0, -- Time before starting cycles (seconds)
    HeightOffset = 0,
    CanKill = true,
    BreakLights = true,
    KillRange = 100,
    BackwardsMovement = false,
    FlickerLights = {
        false, -- Enabled
        2.5, -- Time (seconds)
    },
    Cycles = {
        Min = 8,
        Max = 8
    },
    CamShake = {
        true, -- Enabled
        {5, 15, 0.1, 1}, -- Shake values (don't change if you don't know)
        100, -- Shake start distance (from Entity to you)
    },
    Jumpscare = {
        true, -- Enabled ('false' if you don't want jumpscare)
        {
            Image1 = "https://tr.rbxcdn.com/c42b0239c5bb6062351f2b6c9aa1ea76/420/420/Decal/Png", -- Image1 url
            Image2 = "https://tr.rbxcdn.com/c42b0239c5bb6062351f2b6c9aa1ea76/420/420/Decal/Png", -- Image2 url
            Shake = false,
            Sound1 = {
                0, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Sound2 = {
                5351101493, -- SoundId
                { Volume = 5 }, -- Sound properties
            },
            Flashing = {
                true, -- Enabled
                Color3.fromRGB(6, 38, 135), -- Color
            },
            Tease = {
                false, -- Enabled ('false' if you don't want tease)
                Min = 1,
                Max = 3,
            },
        },
    },
    CustomDialog = {"Bạn Đã Chết Bởi Thực Thể Blood Ambush", "Blood Ambush Sở Hữu Tốc Độ Di Chuyển Nhanh Hơn Và Ngoại Hình Đáng Sợ Hơn Ambush Doors", "Hãy Vận Dụng Kiến Thức Đã Học Được Về Cách Vượt Qua Ambush Doors Để Vượt Qua Blood Ambush"}, -- Custom death message (can be as long as you want)
})

-----[[ Advanced ]]-----

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity:", entity, "has entered room:", room)
    Speed = 0
    wait(2)
    Speed = 170
end

entity.Debug.OnEntitySpawned = function(entityModel)
    print("Entity has spawned:", entityModel)
local bruh = workspace.Rebound.Torso
local TweenService = game:GetService("TweenService")
local spawn = Instance.new("Sound")
spawn.Parent = bruh
spawn.Name = "ReboundSpawn"
spawn.SoundId = "rbxassetid://9114221327"
spawn.Volume = 5
spawn.RollOffMaxDistance = 10000
spawn.RollOffMinDistance = 450
spawn:Play()
    local move = GetGitSound("https://github.com/check78/worldcuuuup/blob/main/DoomBegin.mp3?raw=true","Reboun")
    move.Parent = bruh
    move.Name = "ReboundMoving"
    move.Volume = 0
    move.Looped = true
local vroom = TweenService:Create(move, TweenInfo.new(2),{Volume = 0.2})
    local distort = Instance.new("DistortionSoundEffect")
    distort.Level = 0.75
    distort.Parent = move
	move.RollOffMaxDistance = 200
	move.RollOffMinDistance = 100
    local tree = Instance.new("TremoloSoundEffect")
    tree.Depth = 1
    tree.Duty = 1
    tree.Frequency = 5
    tree.Parent = move
    local eq = Instance.new("EqualizerSoundEffect")
    eq.HighGain = -60
    eq.MidGain = 10
    eq.LowGain = 10
    eq.Parent = move
	wait(1)
    vroom:Play()
	move:Play()
end

entity.Debug.OnEntityDespawned = function(entityModel)
    print("Entity has despawned:", entityModel)
end

entity.Debug.OnEntityStartMoving = function(entityModel)
    print("Entity has started moving:", entityModel)
end

entity.Debug.OnEntityFinishedRebound = function(entityModel)
    print("Entity finished rebound:", entityModel)
end

entity.Debug.OnDeath = function()
    warn("You died.")

    firesignal(game.ReplicatedStorage.EntityInfo.DeathHint.OnClientEvent, {"Bạn Đã Chết Bởi Thực Thể Blood Ambush", "Blood Ambush Sở Hữu Tốc Độ Di Chuyển Nhanh Hơn Và Ngoại Hình Đáng Sợ Hơn Ambush Doors", "Hãy Vận Dụng Kiến Thức Đã Học Được Về Cách Vượt Qua Ambush Doors Để Vượt Qua Blood Ambush"}, "Yellow")

	local ReSt = game.ReplicatedStorage
	local Plr = game.Players.LocalPlayer

		scare:Play()
	    ReSt.GameStats["Player_".. Plr.Name].Total.DeathCause.Value = "Rebound"
		wait(0.5)
		scare:Destroy()

end

------------------------

-- Run the created entity
Creator.runEntity(entity)
--
-- Repeat Entity Script
while true do
wait(15)
	loadstring(game:HttpGet("https://raw.githubusercontent.com/ZinXC/Entities/main/A-50"))()
	wait(45)
	-- Loadstring Rebound
	loadstring(game:HttpGet("https://raw.githubusercontent.com/idontknowwhatmyname/blank/main/Rebound"))()
	wait(30)
	loadstring(game:HttpGet("https://raw.githubusercontent.com/ZinXC/Entities/main/A-50"))()
	wait(20)
	-- Loadstring Creepy Teeth
	loadstring(game:HttpGet("https://raw.githubusercontent.com/idontknowwhatmyname/blank/main/Creepy%20Teeth"))()
	wait(20)
	loadstring(game:HttpGet("https://raw.githubusercontent.com/ZinXC/Entities/main/A-50"))()
	wait(10)
	-- Loadstring Savage Smile
	loadstring(game:HttpGet("https://raw.githubusercontent.com/idontknowwhatmyname/blank/main/Savage%20Smile"))()
	wait(10)
	loadstring(game:HttpGet("https://raw.githubusercontent.com/ZinXC/Entities/main/A-50"))()
	wait(65)
	-- Loadstring Depth
	loadstring(game:HttpGet("https://raw.githubusercontent.com/idontknowwhatmyname/blank/main/Depth"))()
	wait(10)
	loadstring(game:HttpGet("https://raw.githubusercontent.com/ZinXC/Entities/main/A-50"))()
	-- Loadstring BloodEyes
	wait(15)
	game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
	loadstring(game:HttpGet("https://raw.githubusercontent.com/idontknowwhatmyname/blank/main/BloodEyes"))()
	wait(15)
	game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
	loadstring(game:HttpGet("https://raw.githubusercontent.com/idontknowwhatmyname/blank/main/BloodEyes"))()
	-- Loadstring Blood Ambush
	wait(60)
	loadstring(game:HttpGet("https://raw.githubusercontent.com/idontknowwhatmyname/blank/main/Blood%20Ambush"))()
	wait(45)
	loadstring(game:HttpGet("https://raw.githubusercontent.com/idontknowwhatmyname/blank/main/Depth"))()
	wait(15)
	game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
	loadstring(game:HttpGet("https://raw.githubusercontent.com/idontknowwhatmyname/blank/main/BloodEyes"))()
	wait(5)
	game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
	loadstring(game:HttpGet("https://raw.githubusercontent.com/idontknowwhatmyname/blank/main/BloodEyes"))()
	wait(5)
	game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
	loadstring(game:HttpGet("https://raw.githubusercontent.com/idontknowwhatmyname/blank/main/BloodEyes"))()
	wait(5)
	game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
	loadstring(game:HttpGet("https://raw.githubusercontent.com/idontknowwhatmyname/blank/main/BloodEyes"))()
	wait(5)
	game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
	loadstring(game:HttpGet("https://raw.githubusercontent.com/idontknowwhatmyname/blank/main/BloodEyes"))()
	wait(15)
	game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
	loadstring(game:HttpGet("https://raw.githubusercontent.com/idontknowwhatmyname/blank/main/BloodEyes"))()
	wait(75)
	loadstring(game:HttpGet("https://raw.githubusercontent.com/idontknowwhatmyname/blank/main/Savage%20Smile"))()
	wait(33)
	loadstring(game:HttpGet("https://raw.githubusercontent.com/ZinXC/Entities/main/A-50"))()
end

